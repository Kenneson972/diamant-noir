# @heroui/react v3 declares tailwindcss@>=4 and other peers while this app stays on Tailwind 3
# with prebuilt /public/heroui-v3.min.css. Vercel runs `npm install` without --legacy-peer-deps
# unless this file is present.
legacy-peer-deps=true
