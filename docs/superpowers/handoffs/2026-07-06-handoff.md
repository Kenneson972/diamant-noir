# Handoff — État du projet Kayvila au 2026-07-06

> Document destiné à une autre IA pour reprendre le travail sans perdre le contexte.

---

## Infos essentielles

- **Projet** : Kayvila — conciergerie de villas de standing en Martinique
- **Stack** : Next.js 14 App Router, Supabase PostgreSQL, Tailwind CSS, Resend (emails), Stripe Connect, n8n (chatbots), Recharts, Playwright, Vitest
- **Domaine prod** : `https://kayvila.com`
- **Dev server** : port 3001 (`npm run dev`) — **NE JAMAIS lancer `npm run build`** (corrompt le cache)
- **Repo** : `Kenneson972/diamant-noir` sur GitHub, branche `main`
- **Dossier** : `/Users/kennesonbasel-somnier/Downloads/CLIENT KARIBLOOM/DIAMANTNOIR/diamant-noir`
- **Tests** : 186 tests Vitest, Playwright sur port 3001

---

## État actuel — Ce qui est FAIT

### 1. Vue 360 Revenus Admin ✅
- Page `/admin/revenus` : graphique 12 mois cliquable, panneau détail par mois
- CA, commission décomposée, ADR, occupation, annulé/pipeline, tableaux villa/canal
- Export CSV par mois, badges comparaison MoM/YoY
- 40 tests, fichier `lib/revenue/monthly-detail.ts`

### 2. Nettoyage textes espace client ✅
- Suppression des fils d'ariane redondants
- Kickers dorés personnalisés par page (10 routes)
- Suppression du composant `PageTopbar`
- 10 pages modifiées

### 3. Emails — Debug + Unification ✅
- **Bug corrigé** : `RESEND_FROM_EMAIL` formaté (`Kayvila <conciergerie@kayvila.com>` au lieu de `<conciergerie@kayvila.com>`)
- **Bug corrigé** : `NEXT_PUBLIC_BASE_URL` = `https://kayvila.com` (était `diamant-noir.vercel.app`)
- **4 nouveaux templates** React Email créés, **0 HTML inline** restant
- 20 emails au total, tous avec layout Kayvila (logo, ligne or, bannière)

### 4. Revenus Propriétaire ✅
- Graphique 6 mois, commission source (20% OTA / 22% direct), fallback `total_price_cents`
- Cohérent avec la page admin et le KPI dashboard

### 5. Fix Realtime Supabase ✅
- Anti-double souscription dans 3 composants de chat
- Guard `getChannels().find()` avant création de canal

### 6. Agents IA — FAQ + Fallbacks ✅
- **FAQ centralisée** : 30 entrées en 4 catégories (`lib/chatbot/faq.ts`)
- **3 fallbacks offline** : public (`lib/chatbot/public-fallback.ts`), admin (`lib/admin-chat-fallback.ts`), proprio (`lib/owner-assistant-reply.ts`)
- **Route digest proprio créée** (bug P0 — n'existait pas)
- **3 context routes mises à jour** (visitor, admin, owner) avec FAQ + insights
- **n8n workflows générés** dans `~/Downloads/KAYVILABOT/` (v4/v5)
- **Migration Supabase** : `20260705130000_chatbot_feedback.sql` à appliquer

### 7. Guide utilisateur ✅
- `docs/guide-kayvila.md` — 679 lignes, 3 espaces, glossaire, FAQ

### 8. Fiche villa — Réorganisation façon Airbnb ✅ (mergé + pushé)
- Brainstorm → spec (`docs/superpowers/specs/2026-07-06-villa-detail-airbnb-layout-design.md`) → plan (`docs/superpowers/plans/2026-07-06-villa-detail-airbnb-layout.md`) → exécution subagent-driven (5 tâches) → revue finale → merge dans `main` (fast-forward) → push
- Nouvel ordre de section sur `app/villas/[id]/page.tsx` : Expérience Kayvila → Description → Chambres → Équipements → Calendrier → Avis → Alentours (carte) → Hôte → À savoir → Concierge → Questions
- **Équipements** : nouveau composant `components/villas/VillaAmenitiesPreview.tsx` — aperçu plat (10 items) + bouton "Voir les N équipements" → modale catégorisée (réutilise `LegalModal`, aucune nouvelle dépendance). Logique pure testée dans `lib/villa-amenities-preview.ts` (5 tests)
- **À savoir** : `components/villas/VillaAccordionInfo.tsx` — accordéon replié → 3 colonnes toujours visibles
- Style Kayvila (navy/or, coins carrés) intact, aucun changement esthétique majeur
- **Bug critique trouvé et corrigé en testant en vrai dans le navigateur** (invisible à `tsc`) : `getEquipmentIcon` était exporté d'un fichier `"use client"` et appelé depuis `page.tsx` (Server Component) → erreur Next.js RSC. Déplacé vers `lib/villa-amenities-preview.ts` (module neutre)
- 202/202 tests passent (7 commits, revue finale : "ready to merge")

### 9. Carte bloquée (CSP) + bandeau CTA recoloré ✅ (commité, pas encore pushé)
- **Carte Google Maps bloquée** : la section "Alentours" pointe vers `https://www.google.com/maps?...&output=embed`, absent de l'allowlist CSP `frame-src`. Ajouté dans `next.config.mjs`
- **Bandeau "Prêt à réserver ?"** : fond navy → fond clair doré (`bg-gold/10`), texte navy, bouton solide navy/blanc + hover doré (cohérent avec le pattern de bouton déjà utilisé partout ailleurs sur le site)
- Commit `81804f7` — **local main est en avance de 1 commit sur origin, à pousser**

---

## Ce qui reste À FAIRE

### ⚠️ Priorité — Stripe Live
1. **Vercel → Settings → Environment Variables (Production)** :
   - `STRIPE_SECRET_KEY` → `sk_live_…`
   - `STRIPE_WEBHOOK_SECRET` → `whsec_…` (endpoint n°1, paiements)
   - `STRIPE_CONNECT_WEBHOOK_SECRET` → `whsec_…` (endpoint n°2, Connect)
2. **Redeploy** Vercel
3. **Tester** : Stripe Dashboard → endpoint → événement test → 200
4. **Fichier de référence** : `docs/stripe-live-checklist.md`

### ⚠️ Priorité — n8n (mis à jour : fix latence inclus)
1. Importer les 3 fichiers depuis `~/Downloads/KAYVILABOT/` dans n8n (Import Workflow) — **re-patchés le 2026-07-06** pour corriger la lenteur des chatbots (voir ci-dessous)
2. Les fichiers sont : `Kayvibot A — Visiteur (v4 — prompt API).json`, `Kayvibot B — Proprietaire (v5 — fixes audit).json`, `Kayvibot C — Admin (v5 — fixes audit).json`

#### Fix lenteur chatbots (2026-07-06)
Diagnostic confirmé par test réel : `/api/chat` timeout à 10,6s (bascule silencieuse sur la FAQ offline), bot proprio documenté à ~20-22s. Cause : chaque bot n8n refaisait un appel HTTP vers `kayvila.com/api/agent/*-context` pour reconstruire un contexte que l'app envoyait déjà dans le payload initial.
- Next.js (**codé, ⚠️ pas encore commité**) : `app/api/chat/route.ts`, `app/api/concierge/owner/route.ts`, `app/api/concierge/admin/route.ts` construisent maintenant `context`+`systemPrompt` en process et les envoient directement à n8n. Nouveaux fichiers : `lib/chatbot/system-prompt.ts`, `lib/owner-assistant-system-prompt.ts`, `lib/admin-assistant-system-prompt.ts`. `lib/admin-assistant-context.ts` a gagné `buildAdminAgentPayload()`. Routes `/api/agent/*-context` conservées (compat/debug) et réutilisent les mêmes fonctions. 197 tests passent, aucune régression.
  - **Fichiers modifiés/créés en attente de commit** : `app/api/agent/{admin,owner,visitor}-context/route.ts`, `app/api/chat/route.ts`, `app/api/concierge/{admin,owner}/route.ts`, `lib/admin-assistant-context.ts`, `types/chatbot.ts` (modifiés) + `lib/admin-assistant-system-prompt.ts`, `lib/chatbot/system-prompt.ts`, `lib/owner-assistant-system-prompt.ts` (nouveaux, untracked)
- n8n (fait, à importer) : les 3 JSON ont été patchés — node "Fetch ...Context" supprimé, "Build Context" lit `body.context`/`body.systemPrompt` du webhook directement. Pour B/C : nodes `Check Auth`/`IF - Non Autorisé ?`/`Respond - Non Autorisé` supprimés (redondants, Next.js gate déjà l'auth avant l'appel n8n) ; `Fetch Owner Data` utilise désormais `$('Edit Fields').userId`.
- **Ce fix ne prend effet qu'après réimport des 3 fichiers dans n8n.**
- Bonus : clé API Resend trouvée en clair dans l'ancien JSON du Bot B — à régénérer une fois migré vers des credentials n8n propres.

### ✅ Priorité — Supabase (fait le 2026-07-06)
Migration `20260705130000_chatbot_feedback.sql` appliquée via MCP Supabase sur le projet DIAMANT NOIR (`wsdawdxucyuyopkpgjij`). Table `chatbot_feedback` créée, RLS activée, index créés.

### ⚠️ Priorité — Vercel (variable manquante)
1. Ajouter `OWNERS_DIGEST_SECRET` = `6c069017abf6882581fa4fe0b5d73e68e8c1400b43aa26b05b9d6595095def3b` (dernière valeur générée)

### ⚠️ Priorité — Git
1. Committer le fix latence chatbot (fichiers listés section 6 ci-dessus, jamais commités)
2. Pousser le commit `81804f7` (fix CSP carte + bandeau CTA) — local `main` en avance de 1 sur origin

### En attente
- Bugs de données espace client : email `locataire@test.com` → vrai email, prix identiques 8 250 € pour 2 séjours
- Audit redirections (domaines en dur, tests Playwright)
- Re-valider `tests/stripe-webhooks.spec.ts` (13/14)
- Endpoint n°2 Connect : ajouter `account.application.deauthorized` si trouvable

---

## Architecture rapide

```
app/
  (admin)/admin/    → dashboard admin (villas, resas, revenus, proprio, clients, soumissions)
  (proprio)/dashboard/ → dashboard proprio (villas, resas, revenus, stats, taches)
  espace-client/    → espace voyageur (sejour, livret, favoris, messages, etc.)
  api/chat/route.ts → chatbot public (fallback FAQ offline)
  api/agent/        → contextes pour bots n8n (visitor, admin, owner, owners-digest)

lib/
  revenue/          → calculs CA, commission, ADR, occupation
  chatbot/          → FAQ, fallbacks, feedback
  emails/           → envoi emails (Resend)
  stripe/           → Stripe Connect (createAccount, onboarding, calculateTransferAmounts)

emails/             → templates React Email (12 fichiers)
components/
  chatbot/          → Chatbot.tsx (widget visiteur)
  dashboard/        → composants admin/proprio partagés
  espace-client/    → composants client
```

## Fichiers clés à connaître

| Fichier | Rôle |
|---------|------|
| `lib/revenue/booking-revenue.ts` | Fonctions de calcul CA/commission (source unique) |
| `lib/revenue/monthly-detail.ts` | Agrégation mensuelle admin (buildMonthlyDetails) |
| `lib/chatbot/faq.ts` | FAQ centralisée + moteur de matching |
| `lib/emails/send.ts` | Tous les emails transactionnels |
| `app/api/chat/route.ts` | Route chatbot publique (fallback + n8n) |
| `app/(admin)/admin/revenus/page.tsx` | Page revenus admin |
| `components/dashboard/admin/AdminRevenusClient.tsx` | UI revenus admin (graphique + panneau) |
| `components/dashboard/admin/RevenueMonthDetail.tsx` | Panneau détail mensuel |
| `docs/stripe-live-checklist.md` | Checklist passage en production Stripe |
| `docs/guide-kayvila.md` | Guide utilisateur complet |
| `scripts/n8n-apply-fixes.py` | Script génération workflows n8n |
| `app/villas/[id]/page.tsx` | Fiche villa publique — ordre de sections façon Airbnb |
| `components/villas/VillaAmenitiesPreview.tsx` | Aperçu équipements + modale catégorisée |
| `lib/villa-amenities-preview.ts` | Logique pure aperçu équipements + `getEquipmentIcon` (module neutre, ni client ni serveur — ne jamais y ajouter `"use client"`) |
| `components/villas/VillaAccordionInfo.tsx` | Section "À savoir", 3 colonnes à plat |
| `next.config.mjs` | Headers CSP — `frame-src` doit lister chaque domaine d'iframe utilisé (Google Maps, Stripe, etc.) |

## Règles importantes

- Ne jamais coder en dur un nom, email, téléphone ou URL — utiliser les variables d'env
- Commission : 22% nuitées (direct) / 20% (OTA) + 100% frais ménage/service
- Les emails utilisent `render()` React Email, jamais de HTML inline
- Les fonctions de calcul sont dans `lib/revenue/booking-revenue.ts` — ne pas les dupliquer
- Dev server port 3001, jamais `npm run build`
- Commits en français, format `type(scope): message`
