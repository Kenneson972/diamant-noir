# SEO Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corriger 15+ problèmes SEO — titres en doublon "Kayvila | Kayvila", sitemap cassé, OG images inexistantes, robots IA manquants, FAQPage/BreadcrumbList JSON-LD.

**Architecture:** Corrections chirurgicales uniquement — pas de refacto, pas de nouveau fichier, pas de modification du layout racine ni des JSON-LD existants.

**Tech Stack:** Next.js 15 App Router, TypeScript

## Global Constraints

- `app/layout.tsx:60` → `template: "%s | Kayvila"` — INTACT. Ne pas toucher.
- `metadataBase` intact. Ne pas modifier.
- `canonical` existants intacts. Ne pas modifier.
- JSON-LD existants (Organization, WebSite, LocalBusiness, VacationRental) intacts.
- `generateMetadata()` — juste corriger les titres, pas refacto.
- `revalidate` et ISR intacts.
- Tous les `title:` de metadata NE DOIVENT PAS contenir "Kayvila".

---

## Vague 1 — P0 bloquants

### Task 1: Fix sitemap — colonne updated_at → created_at

**Files:** Modify: `app/sitemap.ts:35,40`

- [ ] Ligne 35: `.select("id, updated_at")` → `.select("id, created_at")`
- [ ] Ligne 40: `v.updated_at ? new Date(v.updated_at)` → `v.created_at ? new Date(v.created_at)`
- [ ] Commit: `fix(sitemap): updated_at → created_at (colonne inexistante)`

### Task 2: Fix villa detail — title + BreadcrumbList

**Files:** Modify: `app/villas/[id]/page.tsx:56-72, ~377`

- [ ] Ligne 60: `title: \`${data.name} — Kayvila\`` → `title: data.name.length > 60 ? data.name.slice(0, 57) + "..." : data.name`
- [ ] Ligne 72: `title: "Villa — Kayvila"` → `title: "Villa"`
- [ ] Ligne 56: `title: "Villa"` → `title: "Villa"`
- [ ] Après le JSON-LD VacationRental existant, ajouter BreadcrumbList JSON-LD :

```tsx
{/* JSON-LD BreadcrumbList */}
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify({
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      itemListElement: [
        { "@type": "ListItem", position: 1, name: "Accueil", item: "https://kayvila.com" },
        { "@type": "ListItem", position: 2, name: "Villas", item: "https://kayvila.com/villas" },
        { "@type": "ListItem", position: 3, name: villa.name },
      ],
    }),
  }}
/>
```

- [ ] Commit: `fix(villas): title sans Kayvila + BreadcrumbList JSON-LD`

### Task 3: Fix FAQ — title + OG image + FAQPage schema

**Files:** Modify: `app/faq/page.tsx:12,17` + ajout script

- [ ] Ligne 12: `"FAQ | Conciergerie Kayvila — Martinique"` → `"FAQ — Conciergerie en Martinique"`
- [ ] Ligne 17: `og-image.jpg` → `og-default.jpg`
- [ ] Ajouter le JSON-LD FAQPage avant `<LandingShell>` :

```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify({
      "@context": "https://schema.org",
      "@type": "FAQPage",
      mainEntity: CONCIERGERIE_FAQ.flatMap(theme =>
        theme.items.map(({ q, a }) => ({
          "@type": "Question",
          name: q,
          acceptedAnswer: { "@type": "Answer", text: a },
        }))
      ),
    }),
  }}
/>
```

- [ ] Commit: `fix(faq): title, OG image, FAQPage JSON-LD`

### Task 4: Fix prestations layout — title

**Files:** Modify: `app/prestations/layout.tsx:5`

- [ ] Ligne 5: `"Nos Prestations | Kayvila — Conciergerie Privée Martinique"` → `"Nos Prestations — Conciergerie Privée Martinique"`
- [ ] Commit: `fix(prestations): title sans Kayvila`

### Task 5: Fix prestations services [slug] — title + OG title

**Files:** Modify: `app/prestations/services/[slug]/page.tsx:62,66`

- [ ] Ligne 62: `\`${d.title} | Prestations — Kayvila\`` → `\`${d.title} | Prestations\``
- [ ] Ligne 66: `\`${d.title} | Kayvila\`` → `\`${d.title}\``
- [ ] Commit: `fix(prestations): titles service sans Kayvila`

### Task 6: Fix contact layout — OG title

**Files:** Modify: `app/contact/layout.tsx:8`

- [ ] Ligne 8: `"Contact | Kayvila"` → `"Contact"`
- [ ] Commit: `fix(contact): OG title sans Kayvila`

### Task 7: Fix soumettre-ma-villa — title + OG image

**Files:** Modify: `app/soumettre-ma-villa/page.tsx:8,13`

- [ ] Ligne 8: `"Confier ma villa — Kayvila Conciergerie"` → `"Confier ma villa — Conciergerie"`
- [ ] Ligne 13: `og-image.jpg` → `og-default.jpg`
- [ ] Commit: `fix(soumettre): title sans Kayvila + OG image`

---

## Vague 2 — P1

### Task 8: Fix robots.ts — ajout bots IA

**Files:** Modify: `app/robots.ts` (remplacer entièrement)

```ts
import { MetadataRoute } from "next";

const baseUrl = "https://kayvila.com";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: "*",
        allow: "/",
        disallow: ["/admin/", "/dashboard/", "/login", "/api/", "/espace-client/", "/success", "/share/"],
      },
      { userAgent: "ChatGPT-User", allow: "/", disallow: ["/admin/", "/dashboard/", "/login", "/api/"] },
      { userAgent: "PerplexityBot", allow: "/", disallow: ["/admin/", "/dashboard/", "/login", "/api/"] },
      { userAgent: "Google-Extended", allow: "/", disallow: ["/admin/", "/dashboard/", "/login", "/api/"] },
    ],
    sitemap: `${baseUrl}/sitemap.xml`,
  };
}
```

- [ ] Commit: `fix(robots): autoriser ChatGPT-User, PerplexityBot, Google-Extended`

### Task 9: Fix success layout — title

**Files:** Modify: `app/success/layout.tsx:4`

- [ ] Ligne 4: `"Réservation confirmée — Kayvila"` → `"Réservation confirmée"`
- [ ] Commit: `fix(success): title sans Kayvila`

### Task 10: Fix update-password layout — title

**Files:** Modify: `app/update-password/layout.tsx:4`

- [ ] Ligne 4: `"Mise à jour du mot de passe — Kayvila"` → `"Mise à jour du mot de passe"`
- [ ] Commit: `fix(update-password): title sans Kayvila`

### Task 11: Fix qui-sommes-nous — OG image

**Files:** Modify: `app/qui-sommes-nous/page.tsx:26`

- [ ] Ligne 26: `og-image.jpg` → `og-default.jpg`
- [ ] Commit: `fix(qui-sommes-nous): OG image → og-default.jpg`

### Task 12: Fix book — OG image

**Files:** Modify: `app/book/page.tsx:16`

- [ ] Ligne 16: `og-image.jpg` → `og-default.jpg`
- [ ] Commit: `fix(book): OG image → og-default.jpg`

### Task 13: Fix villas comparer — OG image

**Files:** Modify: `app/villas/comparer/layout.tsx:9`

- [ ] Ligne 9: `og-image.jpg` → `og-default.jpg`
- [ ] Commit: `fix(comparer): OG image → og-default.jpg`

### Task 14: Fix villas page — OG title

**Files:** Modify: `app/villas/page.tsx:16`

- [ ] Ligne 16: `"Villas de standing en Martinique | Kayvila"` → `"Villas de standing en Martinique"`
- [ ] Commit: `fix(villas): OG title sans Kayvila`

---

## Vague 3 — Dashboard

### Task 15: Fix dashboard titles — retirer "— Kayvila"

**Files:** Modify tous les fichiers dashboard avec "— Kayvila" dans le title :

- `app/(proprio)/dashboard/villas/[villaId]/disponibilites/page.tsx:6` → `"Disponibilités"`
- `app/(proprio)/dashboard/villas/[villaId]/photos/page.tsx:9` → `"Photos"`
- `app/(proprio)/dashboard/villas/[villaId]/page.tsx:12` → `"Modifier la villa"`
- `app/(proprio)/dashboard/villas/nouvelle/page.tsx:7` → `"Ajouter une villa"`
- `app/(proprio)/dashboard/villas/page.tsx:10` → `"Mes Villas"`
- `app/(proprio)/dashboard/taches/page.tsx:8` → `"Tâches"`
- `app/(proprio)/dashboard/reservations/[villaId]/page.tsx:12` → `"Réservations"`
- `app/(proprio)/dashboard/reservations/page.tsx:14` → `"Réservations"`
- `app/(proprio)/dashboard/revenus/page.tsx:16` → `"Revenus"`
- `app/(proprio)/dashboard/statistiques/[villaId]/page.tsx:22` → `"Statistiques"`
- `app/(proprio)/dashboard/statistiques/page.tsx:8` → `"Statistiques"`

- [ ] Commit: `fix(dashboard): tous les titles sans — Kayvila`

---

## Vérification post-déploiement

```bash
# Sitemap doit avoir 15+ URLs
curl -s https://kayvila.com/sitemap.xml | grep -c '<loc>'

# Titles sans doublon
curl -s https://kayvila.com/faq | grep '<title>'
curl -s https://kayvila.com/prestations | grep '<title>'
curl -s https://kayvila.com/villas/4ce2e4f4-2101-485c-ba8a-0d76d4dcb99a | grep '<title>'

# Robots avec règles IA
curl -s https://kayvila.com/robots.txt

# FAQPage schema présent
curl -s https://kayvila.com/faq | grep -c 'FAQPage'
```
