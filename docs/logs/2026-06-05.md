# Session 2026-06-06 — Correctifs audit dashboard complet

## Périmètre

Exécution intégrale audit `AUDIT_ESPACES_DASHBOARD_2026-06-06.md` (Cursor + Élise).

## Admin

- Revenus : `/api/admin/revenue` + page via `calculateTransferAmounts` (25 % + fees)
- Bookings : POST/PATCH `/api/admin/bookings` (conflit dates, audit log)
- Chat n8n : `select` filtré, plus de `select("*")` brut vers webhook
- Messagerie : schéma `session_id/content/role`, API `/api/admin/messages`
- Pages data : `supabaseAdmin()` dashboard + clients
- Menu : lien Propriétaires ; feedback erreurs avis/demandes/réservations
- `requireAdmin` : cookie session + JWT metadata
- Migration : RLS staff, `assignee_id`, `admin_audit_log`

## Proprio

- IDOR fiche réservation : `verifyVillaOwnerOrAdmin`
- Revenus accueil + copilot : net après commission
- Copilot monté dans layout (`/api/dashboard/owner-assistant`)
- `loading.tsx` / `error.tsx`

## Tenant

- Calendrier → formulaire (`ConnectedAvailabilityCalendar`)
- Loading calendrier + tarifs saisonniers dans `BookingForm`
- Magic link OTP sur `/login`
- Wishlist toggle corrigé ; menu Profil + Checklist
- Message espace vide explicite (email réservation)

## Verify

- Build Next diamant-noir : compile OK (échec typecheck global MAISON PVL hors périmètre)
- Migration Supabase prod `audit_dashboard_fixes` appliquée

---

# Session 2026-06-05 — Mega-fix Kayvila (P0 + P1)

## Contexte

Implémentation du plan `cursor-mega-fix-2026-06-05.md` : 10 bugs P0 et 14 P1 sur le repo `diamant-noir`.

## Phases exécutées

### Phase 1 — Sécurité
- `lib/ota-hub.ts` : correction injection SQL — passage array Supabase pour `.not("external_id", "in", ...)`.
- `app/api/booking/route.ts` : `checkCsrf(request)` avant rate limit.
- `lib/ical-sync.ts` : supprimé (0 import, code mort).

### Phase 2 — Booking
- Migration `20260605120000_bookings_guests.sql` : colonne `guests INTEGER NOT NULL DEFAULT 1`.
- Guards serveur : `is_published`, `min_nights`, capacité via `guestCount = guests ?? 1`.

### Phase 3 — Webhooks Stripe
- Claim atomique en début de handler (`upsert` + `ignoreDuplicates`).
- Handlers ajoutés : `charge.refunded`, `checkout.session.async_payment_failed`, `charge.dispute.closed`, `charge.dispute.funds_reinstated`, `charge.dispute.funds_withdrawn`.
- INSERT final doublon supprimé.

### Phase 4 — Dashboards revenus
- Admin : 5 cartes (CA mois/année/total, commission Kayvila, reversement proprios) + chart double série via `calculateTransferAmounts`.
- Proprio : reversement net (75 % séjour selon `commission_rate` villa) au lieu du brut `total_price_cents`.

### Phase 5 — Résilience booking
- Try/catch création Stripe → rollback booking `cancelled` / `payment_status: failed`.
- Emails fire-and-forget post-session (`send-booking-confirmation`, `notify-admin-booking`) avec `Authorization: Bearer ${API_SECRET_KEY}`.
- Note : double email possible (pending à la création + paid au webhook) — acceptable si templates distinguent le statut.

### Phase 6 — Cleanup & seasonal_rates
- Migration `20260605130000_seasonal_rates.sql` (table + RLS).
- Booking : fetch `seasonal_rates` et passage à `calculatePrice`.
- Hors scope : `SeasonalPricesEditor.tsx` reste sur JSON `villas.seasonal_prices` (tarifs effectifs après seed manuel).

## Config
- `.env.local.example` : `API_SECRET_KEY`, `RESEND_API_KEY`, `BOOKING_CONFIRMATION_WEBHOOK`.

## Vérification
- `npx tsc --noEmit`
- `npm run build`
- `npm run dev` relancé après build

## Supabase (MCP) — migrations appliquées en prod

Projet `wsdawdxucyuyopkpgjij` :

| Version | Nom | Statut |
|---------|-----|--------|
| `20260605151423` | `bookings_guests` | OK — colonne `bookings.guests INTEGER NOT NULL DEFAULT 1` |
| `20260605151432` | `seasonal_rates` | OK — table + index + RLS activé |

Types locaux mis à jour : `types/supabase.ts` (`guests`, `seasonal_rates`).

**Note** : `seasonal_rates` est vide — tarifs effectifs après seed ou branchement de `SeasonalPricesEditor.tsx`.

## RBAC JWT critique (2026-06-06)

Prompt `phase-rbac-jwt-critical.md` (commit `39caf71`) :

- Migration `fix_rls_jwt_role_claim` appliquée Supabase — 0 policy avec `auth.jwt() ->> 'role'` en prod.
- `create-villa` / `delete-villa` : auth proprio + admin (helper `getSessionUser`).
- Source `20260501_rls_audit.sql` corrigée pour futurs déploiements.

## Audit dashboard (2026-06-06)

- Rapport complet : `docs/AUDIT_ESPACES_DASHBOARD_2026-06-06.md`
- 3 audits parallèles : admin (priorité), proprio, tenant
- Top P0 admin : revenus 20 % naïf, RLS bookings sans admin, messagerie schéma, assignee_id

## Migration Hero UI Pro (2026-06-06)

- CLI : `npm exec heroui-pro install --yes` (login @Kenneson972)
- Packages retirés : Radix, FullCalendar, dnd-kit, GSAP, react-day-picker, react-leaflet, markercluster
- Ajoutés : `@heroui/react`, `@heroui/styles`, `@heroui-pro/react`, Tailwind v4
- Calendriers : `AvailabilityCalendar` → `RangeCalendar`, admin/team → `Calendar` + `CellIndicator`
- `ActionMenu` → HeroUI `Dropdown` ; `tabs.tsx` → wrapper HeroUI `Tabs`
- `VillaImageManager` : réordonnancement par flèches (plus de drag)
- Prestations : scroll scrub natif (`use-scroll-scrub`) + CSS `.pvsh-scroll-panel`
- **Exception** : Leaflet gardé pour `VillasMapView` (carte multi-villas interactive)
- `tsconfig` : exclude `Downloads/` et `.worktrees/` (pollution MAISON PVL)

## Restauration polices Kayvila (2026-06-06)

- Git : `font-display` → Playfair (`--font-playfair`), body → Instrument Sans
- Fix : variables `next/font` déplacées sur `<html>` (pas seulement `<body>`) ; utilitaires explicites `.font-display` / `.font-sans` ; `@theme` avant `@heroui/styles`
- Pas de push (demande client)

## Fix Tailwind v4 + HeroUI (2026-06-06)

- Erreur : `@heroui/styles/.../accordion.css` — `Cannot apply unknown utility class text-sm`
- Cause : `@config "../tailwind.config.ts"` (format v3) dans `globals.css` empêchait TW4 de résoudre les utilitaires dans les CSS node_modules HeroUI
- Fix : retirer `@config`, ajouter `@theme { }` avec couleurs/fonts/animations Kayvila (pattern MAISON PVL)
- Vérif : `npm run build` OK

## Retrait magic link login (2026-06-06)

- Suppression état `magicLinkSent` / `magicLoading`, handler `handleMagicLink`, bloc UI sous le formulaire login.
- Fichier : `app/login/page.tsx`
- Magic link post-réservation sur `/success` conservé.

## HeroUI Pro premium — Phase 1 Data Grids (2026-06-06)

- Wrappers Kayvila : `components/ui/pro/{kayvila-data-grid,kayvila-empty-state,kayvila-widget,kayvila-rating}.tsx`
- DataGrids : clients, propriétaires, réservations admin, villas admin, réservations proprio
- Admin overview : `KayvilaWidget` arrivées/départs/occupation (fix balise fermante)
- `KpiCard` → compound `KPI` HeroUI Pro ; `BookingForm` → `NumberStepper`
- `VillaGallery` conservée (mosaïque + lightbox custom — qualité premium existante)
- Phases 2–4 restantes : Carousel, HoverCard, Stepper, Kanban, Command, ActionBar, DropZone, charts Pro
- Vérif : `npm run build` OK

## HeroUI Pro premium — Phase 2 UX client (2026-06-06)

- Pull `0eeaf42` (prompt Élise `cursor-restore-airbnb-villa-design.md`)
- Push `60768ff` phase 1 Data Grids
- Carousel HeroUI : mobile `VillaGallery` via `KayvilaCarousel` (desktop mosaïque + lightbox conservés)
- HoverCard : `VillaListingCard` sur grille `/villas`
- Stepper : `CheckoutView` étape 4/4 (Dates → Voyageurs → Coordonnées → Paiement)
- Rating interactif : `ReviewForm` → `KayvilaRating`
- `BookingForm` NumberStepper déjà en place (phase 1)
- Vérif : `npm run build` OK

## HeroUI Pro premium — Phase 3 Admin productivité (2026-06-06)

- Kanban : `AdminReservationsKanban` — colonnes pending/confirmed/checkin/completed/cancelled, drag `useKanban` → `onStatusChange`
- Command palette : `AdminCommandPalette` dans `DashboardShell` (admin only), ⌘K/Ctrl+K, nav + 12 réservations + 12 villas
- ActionBar : `KayvilaActionBar` sur vue liste réservations (export CSV, confirmer, annuler bulk)
- DataGrid : props `selectedKeys` / `onSelectionChange` sur `KayvilaDataGrid` + `AdminReservationsDataGrid`
- DropZone : `VillaImageManager` réécrit — JPEG/PNG/WebP/AVIF, max 5 Mo, Supabase `villa-images`
- Page réservations : toggle liste / kanban / calendrier
- Fix TS : cast `PipelineColumn` dans `setColumn` Kanban
- Phase 4 restante : EmptyState listes, PressableFeedback, NumberValue
- Vérif : `npm run build` OK

## HeroUI Pro premium — Phase 4 + Prompt Élise Airbnb (2026-06-06)

- Wrappers : `KayvilaNumberValue`, `KayvilaPressableButton` (PressableFeedback.Ripple gold)
- EmptyState : admin avis/demandes/messagerie, espace-client, `VillaReviews`
- NumberValue : `AdminReservationsDataGrid`, `AdminVillasDataGrid`, `AdminReservationsKanban`
- PressableFeedback : `BookingForm`, `PriceCalculator` boutons Réserver
- Élise : grille services à la carte dans section expérience ; icônes gold équipements ; fix avatar `VillaHostCard` (relative + sizes)
- Checklist Élise déjà en place : equipment_*, `getEquipmentIcon`, `VillaHostCard`, WishlistButton, `VillaGallery`/Carousel mobile
- Vérif : `npm run build` OK

## Prompt Élise — 10 finitions client (2026-06-06)

1. **HoverCard → Sheet mobile** : `VillaListingCard` + `useMediaQuery` — preview bottom sheet sur mobile, HoverCard desktop
2. **Filtres minGuests** : `VillaFilterBar` + init depuis `dateQuery.guests` dans `VillasMapView`
3. **Calculateur remboursement** : `lib/refund-policy.ts` + affichage à l’annulation sur fiche réservation
4. **AddToCalendar** : composant réutilisable sur espace-client + fiche réservation
5. **Carte légère** : `VillasMapEmbed` (OSM iframe) remplace Leaflet desktop + mobile
6. **Comparer** : `/villas/comparer?ids=` — page + lien `CompareBar` déjà en place
7. **LocalGuide** : recommandations locales dans espace-client (séjour actif ou passé)
8. **Envie de revenir ?** : section ré-réservation si `pastBookings.length > 0`
9. **Partage UUID** : table `booking_shares`, API `POST /api/booking/share`, page `/share/[token]` (UUID + legacy btoa)
10. **Migration** : `20260606120000_booking_shares.sql` — UNIQUE sur `booking_id` pour upsert

- Vérif : `npm run build` OK

## Refonte checkout + perf navigation (2026-06-06)

### Checkout `/book`
- Prefetch villa côté RSC (`app/book/page.tsx`) — plus de `select("*")` client
- `CheckoutView` réécrit : hero navy, `rounded-none`, `font-display`, liens CGV/terms/confidentialité
- `CheckoutPriceSummary` extrait — récap desktop + compact mobile au-dessus du fold
- CTA sticky mobile avec total ; `KayvilaPressableButton` ; politique alignée `refund-policy`
- `app/book/loading.tsx` skeleton checkout
- Fix TS : type `Session` sur `getSession()`

### Perf navigation (dev)
- `middleware.ts` : skip `getUser()` pour visiteurs anonymes sur pages publiques
- `package.json` : `next dev --turbo`
- `NavigationProgress` + barre dans `globals.css` / `SiteFrame`
- `app/loading.tsx` skeleton léger (nav/footer visibles)

### CSP
- `next.config.mjs` : `https://www.openstreetmap.org` dans `frame-src`

- Vérif : `npm run build` OK

## Refonte design espace client — phases 2 & 3 (2026-06-06)

### Phase 2
- `profil/page.tsx` — widgets Pro, préférences séjour, Chip « Bientôt » documents
- `favoris/page.tsx` — `KayvilaEmptyState`, `VillaCoverImage`, Button retirer favori
- `messagerie/page.tsx` — bannière séjour Chip HeroUI

### Phase 3
- Nouveau `TenantSectionHeader` — en-tête éditorial sous-pages (eyebrow gold + trait)
- `notifications` — widget Pro, empty state, bouton « Tout lire »
- `documents` — widgets séjours + factures, empty state
- `demandes` — widgets formulaire + historique
- `conciergerie` — contacts, horaires, services en widgets Pro
- `checklist` — progression widget + étapes, empty state, Button iCal

- Vérif : `npm run build` OK
- Reste : `livret`, `reservations/[id]`, composants `tenant-ui` (RequestForm, ProfileForm…)

## P0 reviews — fix Élise (2026-06-06, Cursor)

- Migration `20260606190000_reviews_restore_admin_columns.sql` créée (status, photos jsonb, guest_id, updated_at, RLS)
- Prompt `cursor-fix-conciergerie.md` : checklist alignée (VillaHostCard conservé, vrais P0)
- Build OK ; fix collatéral Claude Code : `Chip variant="soft"` dans `AdminOwnersDataGrid` (HeroUI v3 n'a pas `flat`)
- Migration prod : appliquée via MCP Supabase `apply_migration` (`reviews_restore_admin_columns`) — colonnes + RLS confirmées

## Audit Supabase Kayvila + fix villas proprio (2026-06-06, Cursor)

**Cause racine villas vides sur fiche proprio** : colonne `villas.slug` absente en prod → `.select(..., slug, ...)` échouait ; liste OK via vue `admin_owner_summary` (ne demande pas slug).

**Correctifs** :
- Migration `villas_add_slug` appliquée MCP (slug = id en backfill)
- Pages admin propriétaires → `supabaseAdmin()` (comme `/admin/villas`)
- Build OK

**Données prod** : 3 owners, 2 villas liées (Jean Martin + Marie Dubois). `proprio@test.com` = 0 villa (normal).

**Audit sécurité Supabase — traité (2026-06-06)** :
- Migration `20260606200000_admin_supabase_standardize` : `is_staff_admin()` = service_role + JWT metadata + `profiles.role`
- `profiles`, `villas`, `seasonal_rates`, `villa_submissions`, `admin_chat_logs`, `ai_action_logs` alignés
- `seasonal_rates` : policies public read + admin + owner par villa
- `booking_shares` : RLS intentionnel sans policy anon (accès service_role / API uniquement)
- Pages RSC admin migrées : `parametres`, `sync-ota`, `membres/[id]`, `clients/[id]` → `getAdminDb()`
- Fix bug fiche client : bookings via `client_user_id` + `guest_email` (plus `guest_email.eq.${uuid}`)
- Garde-fous : `npm run check:schema`, règle `kb-admin-supabase-diamantnoir.mdc`
- Reste : regen complète `types/supabase.ts` depuis prod ; migrer `demandes`/`avis`/`reservations` client vers `/api/admin/*` (optionnel, RLS OK maintenant)

## Vérif Admin Propriétaires — Claude Code (2026-06-06, Cursor)

- `npm run build` OK — routes `/admin/proprietaires`, `/admin/proprietaires/[id]`, APIs owners
- Migration `admin_owner_view` appliquée prod MCP — vue retourne 3 proprios, `suspended` ajouté
- Dev server actif `localhost:3000`
- Écarts vs spec initiale (lite OK) : pas de CA/occupation/disputes en liste ; revenus sans graph ; commission hardcodée 25% dans `OwnerRevenueTab`
