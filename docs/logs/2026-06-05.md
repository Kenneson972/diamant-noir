# Session 2026-06-06 — Correctifs audit dashboard complet

## Périmètre

Exécution intégrale audit `AUDIT_ESPACES_DASHBOARD_2026-06-06.md` (Cursor + Élise).

## Admin

- Revenus : `/api/admin/revenue` + page via `calculateTransferAmounts` (25 % + fees)
- Bookings : POST/PATCH `/api/admin/bookings` (conflit dates, audit log)
- Chat n8n : `select` filtré, plus de `select("*")` brut vers webhook
- Messagerie : schéma `session_id/content/role`, API `/api/admin/messages`
- Pages data : `supabaseAdmin()` dashboard + clients
- Menu : lien Propriétaires ; feedback erreurs avis/demandes/réservations
- `requireAdmin` : cookie session + JWT metadata
- Migration : RLS staff, `assignee_id`, `admin_audit_log`

## Proprio

- IDOR fiche réservation : `verifyVillaOwnerOrAdmin`
- Revenus accueil + copilot : net après commission
- Copilot monté dans layout (`/api/dashboard/owner-assistant`)
- `loading.tsx` / `error.tsx`

## Tenant

- Calendrier → formulaire (`ConnectedAvailabilityCalendar`)
- Loading calendrier + tarifs saisonniers dans `BookingForm`
- Magic link OTP sur `/login`
- Wishlist toggle corrigé ; menu Profil + Checklist
- Message espace vide explicite (email réservation)

## Verify

- Build Next diamant-noir : compile OK (échec typecheck global MAISON PVL hors périmètre)
- Migration Supabase prod `audit_dashboard_fixes` appliquée

---

# Session 2026-06-05 — Mega-fix Kayvila (P0 + P1)

## Contexte

Implémentation du plan `cursor-mega-fix-2026-06-05.md` : 10 bugs P0 et 14 P1 sur le repo `diamant-noir`.

## Phases exécutées

### Phase 1 — Sécurité
- `lib/ota-hub.ts` : correction injection SQL — passage array Supabase pour `.not("external_id", "in", ...)`.
- `app/api/booking/route.ts` : `checkCsrf(request)` avant rate limit.
- `lib/ical-sync.ts` : supprimé (0 import, code mort).

### Phase 2 — Booking
- Migration `20260605120000_bookings_guests.sql` : colonne `guests INTEGER NOT NULL DEFAULT 1`.
- Guards serveur : `is_published`, `min_nights`, capacité via `guestCount = guests ?? 1`.

### Phase 3 — Webhooks Stripe
- Claim atomique en début de handler (`upsert` + `ignoreDuplicates`).
- Handlers ajoutés : `charge.refunded`, `checkout.session.async_payment_failed`, `charge.dispute.closed`, `charge.dispute.funds_reinstated`, `charge.dispute.funds_withdrawn`.
- INSERT final doublon supprimé.

### Phase 4 — Dashboards revenus
- Admin : 5 cartes (CA mois/année/total, commission Kayvila, reversement proprios) + chart double série via `calculateTransferAmounts`.
- Proprio : reversement net (75 % séjour selon `commission_rate` villa) au lieu du brut `total_price_cents`.

### Phase 5 — Résilience booking
- Try/catch création Stripe → rollback booking `cancelled` / `payment_status: failed`.
- Emails fire-and-forget post-session (`send-booking-confirmation`, `notify-admin-booking`) avec `Authorization: Bearer ${API_SECRET_KEY}`.
- Note : double email possible (pending à la création + paid au webhook) — acceptable si templates distinguent le statut.

### Phase 6 — Cleanup & seasonal_rates
- Migration `20260605130000_seasonal_rates.sql` (table + RLS).
- Booking : fetch `seasonal_rates` et passage à `calculatePrice`.
- Hors scope : `SeasonalPricesEditor.tsx` reste sur JSON `villas.seasonal_prices` (tarifs effectifs après seed manuel).

## Config
- `.env.local.example` : `API_SECRET_KEY`, `RESEND_API_KEY`, `BOOKING_CONFIRMATION_WEBHOOK`.

## Vérification
- `npx tsc --noEmit`
- `npm run build`
- `npm run dev` relancé après build

## Supabase (MCP) — migrations appliquées en prod

Projet `wsdawdxucyuyopkpgjij` :

| Version | Nom | Statut |
|---------|-----|--------|
| `20260605151423` | `bookings_guests` | OK — colonne `bookings.guests INTEGER NOT NULL DEFAULT 1` |
| `20260605151432` | `seasonal_rates` | OK — table + index + RLS activé |

Types locaux mis à jour : `types/supabase.ts` (`guests`, `seasonal_rates`).

**Note** : `seasonal_rates` est vide — tarifs effectifs après seed ou branchement de `SeasonalPricesEditor.tsx`.

## RBAC JWT critique (2026-06-06)

Prompt `phase-rbac-jwt-critical.md` (commit `39caf71`) :

- Migration `fix_rls_jwt_role_claim` appliquée Supabase — 0 policy avec `auth.jwt() ->> 'role'` en prod.
- `create-villa` / `delete-villa` : auth proprio + admin (helper `getSessionUser`).
- Source `20260501_rls_audit.sql` corrigée pour futurs déploiements.

## Audit dashboard (2026-06-06)

- Rapport complet : `docs/AUDIT_ESPACES_DASHBOARD_2026-06-06.md`
- 3 audits parallèles : admin (priorité), proprio, tenant
- Top P0 admin : revenus 20 % naïf, RLS bookings sans admin, messagerie schéma, assignee_id

## Migration Hero UI Pro (2026-06-06)

- CLI : `npm exec heroui-pro install --yes` (login @Kenneson972)
- Packages retirés : Radix, FullCalendar, dnd-kit, GSAP, react-day-picker, react-leaflet, markercluster
- Ajoutés : `@heroui/react`, `@heroui/styles`, `@heroui-pro/react`, Tailwind v4
- Calendriers : `AvailabilityCalendar` → `RangeCalendar`, admin/team → `Calendar` + `CellIndicator`
- `ActionMenu` → HeroUI `Dropdown` ; `tabs.tsx` → wrapper HeroUI `Tabs`
- `VillaImageManager` : réordonnancement par flèches (plus de drag)
- Prestations : scroll scrub natif (`use-scroll-scrub`) + CSS `.pvsh-scroll-panel`
- **Exception** : Leaflet gardé pour `VillasMapView` (carte multi-villas interactive)
- `tsconfig` : exclude `Downloads/` et `.worktrees/` (pollution MAISON PVL)

## Restauration polices Kayvila (2026-06-06)

- Git : `font-display` → Playfair (`--font-playfair`), body → Instrument Sans
- Fix : variables `next/font` déplacées sur `<html>` (pas seulement `<body>`) ; utilitaires explicites `.font-display` / `.font-sans` ; `@theme` avant `@heroui/styles`
- Pas de push (demande client)

## Fix Tailwind v4 + HeroUI (2026-06-06)

- Erreur : `@heroui/styles/.../accordion.css` — `Cannot apply unknown utility class text-sm`
- Cause : `@config "../tailwind.config.ts"` (format v3) dans `globals.css` empêchait TW4 de résoudre les utilitaires dans les CSS node_modules HeroUI
- Fix : retirer `@config`, ajouter `@theme { }` avec couleurs/fonts/animations Kayvila (pattern MAISON PVL)
- Vérif : `npm run build` OK

## Retrait magic link login (2026-06-06)

- Suppression état `magicLinkSent` / `magicLoading`, handler `handleMagicLink`, bloc UI sous le formulaire login.
- Fichier : `app/login/page.tsx`
- Magic link post-réservation sur `/success` conservé.
