# Session 2026-04-11

## Login / inscription mobile

- **Problème** : bandeau vidéo/image fixe à 200px ; formulaire inscription plus haut → ligne nette entre média et fond clair.
- **Changement** : `app/login/page.tsx` — `h-[clamp(220px,42svh,420px)]` + dégradé bas de bandeau vers blanc (mobile uniquement).

## Prestations — overlays vidéo scroll

- **Changement** : `components/prestations/VideoScrollHero.tsx` — cartes latérales : fond navy opaque, bordures or, typo remontée (sm / white/90), plus de backdrop-blur sur les cartes.

## Prestations — hub + Finance505–560

- **`PrestationsPageClient.tsx`** : hub `#piliers` (5 cartes → `/prestations/services/*`), popups **Voir le détail** en navigation client, CTAs vers `#piliers`.
- **`VideoScrollHero.tsx`** : même calage Finance **505–560**, liens service en `href` route (plus de scroll `#inclusions`).
- Aligné avec `data/prestations-scroll-sections.ts` (commentaire café + tablette).
