### 2026-04-01T00:30:00Z — Cursor — Dashboard proprio premium éditorial

- **type**: `ui`
- **summary**:
  - Aligner l'espace propriétaire sur le design système "luxe/éditorial" du site (navy/or, fonts display, filets or).
  - Création d'un layout unifié avec un `ProprioChrome` persistant et d'un set de composants `ProprioPageIntro` / `ProprioSectionHeading`.
  - Application de ce nouveau look aux pages Index, Analytics, Submissions, Assistant et au détail d'une Villa.
  - L'interface Assistant IA passe d'un terminal sombre pur à un style "salon privé" utilisant le Navy et Or.
- **files**:
  - `components/dashboard/proprio/ui.tsx`
  - `app/dashboard/proprio/layout.tsx`
  - `app/dashboard/proprio/page.tsx`
  - `app/dashboard/proprio/analytics/page.tsx`
  - `app/dashboard/proprio/submissions/page.tsx`
  - `app/dashboard/proprio/assistant/page.tsx`
  - `app/dashboard/proprio/[villaId]/page.tsx`
- **why**:
  - Demande client pour un design plus aligné sur le positionnement luxe de la marque au sein de l'espace propriétaire.
- **impact**:
  - Le dashboard est plus immersif et luxueux.
- **verify**:
  - `npm run lint` OK
  - `npm run build` OK

### 2026-03-31T23:35:00Z — Cursor — Refonte Mobile Navbar & Chatbot (phase finale)

- **type**: `ui`
- **summary**:
  - Navbar mobile refondue avec centrage absolu du logo (`left-1/2` via conteneur centré), structure `justify-between` plus robuste contre les collisions latérales.
  - Réduction des actions en topbar mobile: conservation du burger + CTA réserver ; actions secondaires reportées en tablette/desktop.
  - Drawer enrichi avec accès rapides `Appeler le concierge`, `Favoris` (compteur), `Connexion/Espace client` pour garder la topbar légère.
  - Chatbot: FAB moins agressif visuellement (taille/animation adoucies), boutons header uniformisés en hit targets 44x44.
- **files**:
  - `components/layout/Navbar.tsx`
  - `components/chatbot/Chatbot.tsx`
  - `docs/ACTIONS_LOG.md`
  - `docs/logs/2026-03-31.md`
- **why**:
  - Demande utilisateur explicite d’une vraie refonte mobile (pas un simple « shrink » desktop), avec priorité au logo centré et à la lisibilité de la navbar.
- **impact**:
  - Header mobile plus premium, stable et lisible.
  - Navigation secondaire accessible via drawer sans surcharge visuelle.
  - Chatbot mieux intégré aux contraintes mobile (safe-area + ergonomie tactile).
- **verify**:
  - `npm run lint` OK (1 warning existant non bloquant sur `<img>` dans `Chatbot.tsx`).
  - `npm run build` OK.
# Session 2026-03-31

### 2026-03-31T24:58:00Z — Cursor — Revue mobile globale (adaptation réelle, pas simple réduction)

- **type**: `ui`
- **summary**:
  - Socle mobile-first partagé ajouté dans `app/globals.css` : `.page-px`, `.section-y`, `.section-y-compact` (gouttières 16→24, rythme vertical progressif).
  - Shells marketing alignés (`components/marketing/landing-sections.tsx`) + footer (`components/layout/Footer.tsx`) pour uniformiser la hiérarchie mobile (espacement, titres, CTA bands).
  - Parcours publics principaux ajustés (`app/page.tsx`, `app/villas/page.tsx`, `app/villas/[id]/page.tsx`, `app/book/page.tsx`, `app/contact/page.tsx`) : titres moins “desktop”, CTA/tap-targets, densité des grilles mobile.
  - Pages secondaires/légales harmonisées (`app/soumettre-ma-villa/page.tsx`, `app/terms/page.tsx`, `app/confidentialite/page.tsx`, `app/cookies/page.tsx`) : gouttières cohérentes, champs 44px, tailles de texte mobiles.
  - Espace client + dashboard (`app/espace-client/layout.tsx`, `app/espace-client/page.tsx`, `app/espace-client/messagerie/page.tsx`, `app/dashboard/proprio/page.tsx`, `app/dashboard/proprio/analytics/page.tsx`, `app/dashboard/proprio/submissions/page.tsx`) : safe-area header mobile, stats compactes, tableaux scrollables, padding unifiés.
- **why**:
  - Demande explicite de revoir “tout le site” pour une vraie adaptation mobile contextuelle.
- **impact**:
  - Mobile plus lisible et plus utilisable sur 320–390px, moins d’effet “copie réduite”, meilleure cohérence entre pages marketing et surfaces app.
- **verify**:
  - `npm run build` OK
  - `GET http://localhost:3010/` -> `200`
  - `GET http://localhost:3010/villas` -> `200`
  - `GET http://localhost:3010/book` -> `200`
  - `GET http://localhost:3010/contact` -> `200`
  - `GET http://localhost:3010/villas/test-id` -> `404` (attendu)

### 2026-03-31T24:15:00Z — Cursor — Navbar: fix chevauchement logo / téléphone (grille + breakpoint lg)

- **type**: `ui`
- **summary**:
  - Remplacement du bloc logo en `absolute` par une grille `grid-cols-[auto_minmax(0,1fr)_auto]` : la colonne centrale est réservée au wordmark, les actions ne se superposent plus.
  - Numéro en clair : `hidden lg:inline-flex` uniquement ; icône téléphone `inline-flex lg:hidden` sur toutes les largeurs &lt;1024px (plus de texte `md` qui débordait sur le logo en paysage / tablette).
  - Wordmark : `truncate` + `whitespace-normal` sur le lien pour surcharger le `whitespace-nowrap` du composant si l’espace est trop étroit.
- **files**: `components/layout/Navbar.tsx`
- **verify**: `npm run build` OK

### 2026-03-31T23:58:00Z — Cursor — Navbar + chatbot mobile (safe-area, comparaison, tap targets)

- **type**: `ui`
- **summary**:
  - Variables CSS `--safe-area-*` + utilitaires `.safe-top` / `.safe-x` en complément de `.safe-bottom`.
  - Navbar: `safe-top` sur le header fixe et le drawer; marges latérales resserrées sur &lt;380px; conteneur logo `min-w-0` + `max-w` + léger scale sur &lt;360px pour éviter les collisions avec menu / compte / CTA.
  - CompareBar: `z-40` + `safe-x` pour rester sous le FAB/panneau chat (`z-[52]`).
  - Chatbot: FAB avec `right`/`bottom` tenant compte des safe insets; offset vertical quand `compareCount &gt; 0`; panneau plein écran `min-h-0` + `100dvh` + header/input safe-area; suggestions et bouton envoi min 44px sur mobile; textarea 16px sur mobile (évite zoom iOS); badge Gem Lucide au lieu de l’emoji.
- **files**:
  - `app/globals.css`
  - `components/layout/Navbar.tsx`
  - `components/villas/CompareBar.tsx`
  - `components/chatbot/Chatbot.tsx`
- **why**:
  - Demande: responsive mobile « super propre » sur navbar et chatbot.
- **impact**:
  - Hiérarchie des couches fixes claire; FAB accessible au-dessus de la barre de comparaison sans la masquer; confort tactile et lecture sur petits écrans / iOS.
- **verify**:
  - `npm run build` OK

### 2026-03-31T22:40:00Z — Cursor — Tour complet responsive/perf (navbar prioritaire)

- **type**: `ui | perf`
- **summary**:
  - Repassage complet mobile avec focus sur la navbar.
  - Simplification des actions top bar sur très petits écrans et réduction des effets `backdrop-blur` côté mobile.
  - Ajustements dashboard secondaire (headers analytics/submissions) et KPI assistant responsive.
- **files**:
  - `components/layout/Navbar.tsx`
  - `components/layout/BrandLogo.tsx`
  - `app/dashboard/proprio/analytics/page.tsx`
  - `app/dashboard/proprio/submissions/page.tsx`
  - `components/dashboard/assistant-views/StatsView.tsx`
- **why**:
  - Le bug mobile persistait visuellement, surtout sur la barre de navigation.
- **impact**:
  - Navbar plus stable en 320-390px.
  - Meilleure lisibilité des KPI et headers dashboard sur mobile.
  - Coût visuel/perf mobile légèrement réduit (moins de blur en bas/mid range).
- **verify**:
  - `npm run build` OK (après arrêt dev + purge `.next`)
  - `npm run dev -- -p 3000` OK
  - `curl http://localhost:3000/` -> `200`
  - `curl http://localhost:3000/villas` -> `200`
  - `curl http://localhost:3000/dashboard/proprio` -> `200`

### 2026-03-31T22:37:00Z — Cursor — Navbar mobile: passe ciblée + runtime local stabilisé

- **type**: `ui | perf`
- **summary**:
  - Ajustement de la navbar mobile: wordmark plus compact, marges de sécurité revues, simplification des actions sur très petits écrans.
  - Fix du runtime local après erreur 500 due à conflit build/dev sur `.next` (nettoyage cache + relance propre).
- **files**:
  - `components/layout/BrandLogo.tsx`
  - `components/layout/Navbar.tsx`
- **why**:
  - L’utilisateur signalait encore un bug mobile, prioritairement sur le header.
- **impact**:
  - Meilleure lisibilité du header sur petits viewports, réduction des chevauchements dans la barre supérieure.
- **verify**:
  - `npm run build` OK
  - `npm run dev -- -p 3000` relancé après purge `.next`
  - `curl http://localhost:3000/` -> `200`
  - `curl http://localhost:3000/villas` -> `200`

### 2026-03-31T23:25:00Z — Cursor — Correctifs mobile P0/P1 complets

- **type**: `ui | perf`
- **summary**:
  - Implémentation complète du plan mobile P0/P1 sur les surfaces publiques, dashboard proprio et espace-client.
  - Réduction des débordements, réintégration du parcours booking mobile, amélioration des cibles tactiles et de la safe-area sur composants fixes.
- **files**:
  - `app/globals.css`
  - `components/layout/Navbar.tsx`
  - `app/villas/[id]/page.tsx`
  - `components/VillasMapView.tsx`
  - `components/VillaGallery.tsx`
  - `app/dashboard/proprio/assistant/page.tsx`
  - `app/dashboard/proprio/page.tsx`
  - `app/dashboard/proprio/[villaId]/page.tsx`
  - `app/espace-client/layout.tsx`
  - `components/villas/CompareBar.tsx`
  - `components/chatbot/Chatbot.tsx`
  - `app/contact/page.tsx`
- **why**:
  - Le site V1 déployé présentait des régressions mobiles bloquantes sur parcours clés.
- **impact**:
  - UX mobile plus stable (320/375/390px), actions critiques accessibles, meilleure lisibilité dashboard et composants fixes adaptés au bas d’écran.
- **verify**:
  - `npm run build` OK
  - `npm run dev -- -p 3000` actif
  - `curl http://localhost:3000/villas` -> `200`
  - `curl http://localhost:3000/dashboard/proprio` -> `200`
  - Diagnostics IDE sur fichiers modifiés: pas de nouvelles erreurs.

### 2026-03-31T22:40:00Z — Cursor — Hero: priorité “CONCIERGERIE PRIVÉE”

- **type**: `ui`
- **summary**:
  - Ajustement du sous-libellé hero pour placer “CONCIERGERIE PRIVÉE” en tête.
- **files**:
  - `app/page.tsx`
- **why**:
  - Demande utilisateur directe pour renforcer le positionnement marque/service dès l’ouverture.
- **impact**:
  - Message principal du hero plus explicite pour la proposition de valeur.
- **verify**:
  - Relecture visuelle de la ligne éditoriale du hero.

### Cursor — Hero sans titre visible

- « Diamant Noir » en texte géant retiré ; `h1` réservé en `sr-only`.

### Cursor — Hero sans baseline

- Retrait de « Là où l’horizon devient votre seul voisin. » sous le titre.

### Cursor — Hero logo + nav

- Logo agrandi sur l’accueil (`size="hero"`) ; navbar : texte « DIAMANT NOIR » sans pictogramme (`showIcon={false}`).

### Cursor — Copy Martinique

- Retrait Côte d’Azur des fallbacks UI + guide insert villas.

### Cursor — Vidéo hero accueil

- `public/hero.webm` (copie depuis la vidéo client) ; `app/page.tsx` pointe sur ce fichier.

### Cursor — HeroUI v3 espace client

- Réinstallation et branchement des composants sur `/espace-client` uniquement ; feuille `public/heroui-v3.min.css` + `I18nProvider`.
- **Vérif**: `npm run build` OK.

### Cursor — Espace client : fin mode démo + styles HeroUI fiables

- Suppression du mode démo (`dn_demo_bypass`, fausses villas) et du bouton “Démo — sans compte”.
- Chargement de `public/heroui-v3.min.css` via `app/espace-client/head.tsx` (dans le `<head>`), et remplacement de quelques CTA par `Button` HeroUI.
- Ajustement du layout (largeur max + padding) pour une meilleure lisibilité.
- **Vérif**: `rm -rf .next && npm run build` OK ; `npm run dev -p 3000` relancé.

### Cursor — Hardening sécurité + lint non interactif

- `booking-session` : minimisation données (pas de PII), validation `session_id`, rate limit, et réponse seulement si réservation `paid+confirmed`.
- `notifications` (Supabase) : suppression des policies `authenticated` trop permissives (lecture/écriture) — à remplacer par une policy admin explicite.
- Headers sécurité (Referrer-Policy, nosniff, frame options, permissions policy) + `no-store` sur `/api/*`.
- ESLint : ajout config + deps pour rendre le lint exécutable (non interactif).
- **Vérif**: `npm run lint` OK (warnings) ; `npm run build` OK ; `npm run dev -p 3000` relancé.

### Cursor — Next 15 + React 19

- Migration depuis Next 14.2 / React 18 : `await cookies()` dans `app/layout.tsx`, `params` / `searchParams` asynchrones sur les pages serveur concernées, `ChatbotDynamic` client pour `dynamic(..., { ssr: false })`, ajustements TS dashboard villa (`villaId`), `outputFileTracingRoot` dans `next.config.mjs`.
- **Vérif**: `npm run build` OK (Next 15.5.x).

### Cursor — Fix runtime espace client

- **Symptôme**: `TypeError: render is not a function` dans `updateContextConsumer` (pile React DOM).
- **Cause**: `@heroui/react@3.0.1` déclare `peer react >= 19` ; le projet est en React 18.2 / Next 14.2 — incompatibilité des internals de contexte.
- **Changement**: Suppression de `@heroui/react`, remplacement par composants internes (`Card`, `Input`, `Button`, `Skeleton`), simplification `EspaceClientProviders`, suppression du `<link heroui-v3.min.css>` dans `app/espace-client/layout.tsx`.
- **Vérif**: `npm install --legacy-peer-deps` ; `npm run build` OK.

### Cursor — Retrait Chakra/HeroUI (stack UI unifiée)

- Suppression de `@chakra-ui/react` et `@heroui/react` + retrait des assets/links CSS HeroUI (y compris l’injection globale).
- Migration `/espace-client` vers `components/ui` + HTML/Tailwind (plus de `Chip`, `Breadcrumbs`, `Alert`, `ProgressBar`, etc.).
- Retrait `optimizePackageImports` pour HeroUI dans `next.config.mjs`.
- Fix lint: `WelcomeBook` (hooks non conditionnels) + warning deps `Chatbot` ; fix syntaxe `SearchResults`.
- **Vérif**: `npm run build` OK.

### Cursor — Nettoyage assets HeroUI

- Suppression de `public/heroui.min.css` et `public/heroui-v3.min.css` (assets morts après migration).
- **Vérif**: `npm run build` OK.

### Cursor — Mise à jour récap projet

- Mise à jour `RECAP.md` (stack Next 15/React 19, doctrine UI Radix+Tailwind, routes espace client, fiche villa data-driven, note calendrier).
- Alignement `README.md` sur la date et la doctrine UI.

### Claude Code (terminal) — Actions de session (trace)

- Nettoyage dépendances UI : retrait `@chakra-ui/react`, `@emotion/react`, `@heroui/react` + suppression assets HeroUI devenus inutiles.
- Régénération lockfile : `npm install`.
- Validation stabilité : `npm run build` (plusieurs passes durant la session) + relance `npm run dev -- -p 3000`.
- Correctifs associés capturés pendant les builds : syntaxe `components/booking/SearchResults.tsx`, lint hooks (`components/espace-client/WelcomeBook.tsx`, deps `components/chatbot/Chatbot.tsx`).
- Ajustements produit : suppression fallbacks “villa test” (liste `/villas` → empty state si aucune publiée ; détail `/villas/[id]` → `notFound()` si ID absent) ; styles grille minimaux FullCalendar dans `components/booking/AvailabilityCalendar.tsx` pour éviter le calendrier “blanc”.

### 2026-03-31T18:13:30Z — Cursor — Comparatif complet LC vs villas DN + implémentation pilote data-driven

- **type**: `ui | sql | config`
- **summary**:
  - Audit comparatif section par section (référence LC vs page `villas/[id]`) puis implémentation sans régression.
  - Suppression des hardcodes critiques (`surface`, `check-in/out`, sections services/conditions figées) au profit de champs pilotés par données.
  - Ajout des sections manquantes: `Tous les équipements`, `Conditions de réservation` (FAQ), bloc `Des questions...`, et `Recommandées pour vous`.
- **files**:
  - `supabase/migrations/20260331_01_villa_detail_premium_fields.sql`
  - `app/dashboard/proprio/[villaId]/page.tsx`
  - `app/villas/[id]/page.tsx`
  - `components/BookingForm.tsx`
- **why**:
  - Demande utilisateur: comparer en profondeur la page de référence avant amélioration.
  - Objectif: même niveau d’information et de structure premium, sans copier visuellement, en conservant l’existant.
- **impact**:
  - Les fiches villas peuvent afficher des contenus riches personnalisés (services maison/collection, équipements int/ext, conditions de réservation, check-in/out).
  - Le dashboard propriétaire permet maintenant de renseigner ces données sans intervention code.
  - Les villas avec data incomplète restent stables grâce aux fallbacks.
- **verify**:
  - `rm -rf .next && npm run build` OK.
  - `npm run dev -- -p 3000` relancé (port 3000 actif).

### 2026-03-31T18:27:00Z — Cursor — Ajustements demandés: hôte + retrait Collection Signature + UI disponibilités

- **type**: `ui`
- **summary**:
  - Ajout section “Votre hôte” sous la description sur la fiche villa.
  - Suppression de la mise en avant “Collection Signature/Iconic” dans l’interface publique.
  - Refonte visuelle de la zone disponibilité: badges d’horaires check-in/check-out, légende état du calendrier, boutons de navigation stylisés, hover day-cell.
- **files**:
  - `app/villas/[id]/page.tsx`
  - `components/booking/AvailabilityCalendar.tsx`
- **why**:
  - Feedback utilisateur direct sur les éléments manquants et la lisibilité des dates.
- **impact**:
  - Page villa plus conforme à l’attendu UX du client, sans casser la structure déjà implémentée.
- **verify**:
  - Lints locaux sur fichiers modifiés: OK.

### 2026-03-31T22:15:00Z — Cursor — Audit complet + publication V1 (Vercel) + hardening endpoints

- **type**: `security | config | script | ui`
- **summary**:
  - Audit pré-push complet (secrets, CORS, endpoints sensibles, config cron).
  - Ajout `.gitignore` projet pour bloquer `.env*`, `.next/`, `node_modules`, artefacts locaux.
  - Sécurisation route équipe `dashboard/team/[secret]` avec secret serveur (`TEAM_DASH_SECRET`) et `notFound()` sans secret valide.
  - Sécurisation `GET /api/sync` par contrôle `x-vercel-cron` + secret (`CRON_SECRET`/`SYNC_CRON_SECRET`).
  - Restriction CORS `OPTIONS /api/chat` via allowlist d’origines (`NEXT_PUBLIC_BASE_URL`, `VERCEL_URL`, `VERCEL_PROJECT_PRODUCTION_URL`).
  - Ajustement cron Vercel pour plan Hobby (`0 3 * * *`).
  - Déploiement prod réussi sur Vercel + alias actif.
- **files**:
  - `.gitignore`
  - `.env.local.example`
  - `app/dashboard/team/[secret]/page.tsx`
  - `app/api/sync/route.ts`
  - `app/api/chat/route.ts`
  - `vercel.json`
  - `package.json`
  - `package-lock.json`
  - `docs/ACTIONS_LOG.md`
  - `docs/logs/2026-03-31.md`
- **why**:
  - Préparer une V1 publiable en réduisant les risques de sécurité et en garantissant un déploiement démontrable.
- **impact**:
  - URL de démo prod: `https://diamant-noir.vercel.app`.
  - Routes sensibles verrouillées côté prod (route équipe + sync cron + CORS externe chat).
  - Build prod Vercel désormais stable (ajout dépendances manquantes `recharts` + `@dnd-kit/*`).
- **verify**:
  - Local: `npm run build` OK.
  - Vercel: déploiement prod READY + alias `diamant-noir.vercel.app`.
  - Smoke checks HTTP:
    - `/` -> `200`
    - `/villas` -> `200`
    - `/dashboard/team/test` -> `404`
    - `/api/sync` (sans headers cron/secret) -> `403`
    - `OPTIONS /api/chat` avec `Origin` externe -> `403`

### 2026-03-31T23:58:00Z — Cursor — Refonte mobile complète (tout le site)

- **type**: `ui`
- **summary**:
  - Refonte mobile-first appliquée en vagues sur le socle partagé (`globals`, shells marketing, footer, navbar) puis sur les parcours publics clés et les surfaces privées (`espace-client`, dashboard proprio).
  - Navbar mobile finalisée avec logo réellement centré (absolu), densité réduite sous `lg`, actions secondaires déplacées et drawer renforcé (safe areas).
  - Harmonisation des typographies micro sur mobile (labels et CTAs critiques), ajustements de hauteurs hero, simplification de grilles denses et suppression d’effets coûteux sur petits écrans (blur mobile).
  - Renforcement ergonomie tactile: hit targets 44px sur boutons/actions (dashboard, action menus, booking cards, assistant, quick-actions chatbot), calendar admin rendu scroll-safe.
  - Overlays consolidés: FAB chatbot (dimensions valides Tailwind + offset compare bar), compare bar plus utilisable sur mobile (effacer accessible).
- **files**:
  - `app/globals.css`
  - `components/layout/Navbar.tsx`
  - `components/marketing/landing-sections.tsx`
  - `components/layout/Footer.tsx`
  - `app/page.tsx`
  - `app/villas/page.tsx`
  - `components/VillasMapView.tsx`
  - `app/book/page.tsx`
  - `app/villas/[id]/page.tsx`
  - `components/marketing/editorial-blocks.tsx`
  - `app/contact/page.tsx`
  - `app/terms/page.tsx`
  - `app/confidentialite/page.tsx`
  - `app/soumettre-ma-villa/page.tsx`
  - `app/dashboard/proprio/page.tsx`
  - `components/dashboard/ActionMenu.tsx`
  - `components/espace-client/BookingCard.tsx`
  - `app/dashboard/proprio/assistant/page.tsx`
  - `components/AdminCalendar.tsx`
  - `components/chatbot/Chatbot.tsx`
  - `components/espace-client/TenantChatbot.tsx`
  - `components/villas/CompareBar.tsx`
  - `app/dashboard/proprio/analytics/page.tsx`
  - `docs/ACTIONS_LOG.md`
  - `docs/logs/2026-03-31.md`
- **why**:
  - Répondre à la demande “refonte totale mobile” avec adaptation intelligente par contexte (public/app/dashboard), au lieu d’une simple réduction desktop.
- **impact**:
  - Amélioration nette de la lisibilité et de la navigation mobile.
  - Diminution des collisions visuelles (navbar/chatbot/compare bar) et meilleure utilisabilité au pouce.
- **verify**:
  - `npm run lint` OK (warning existant non bloquant sur `<img>` dans `components/chatbot/Chatbot.tsx`).
  - `npm run build` OK.

### 2026-03-31T23:31:00Z — Cursor — Fix final navbar mobile (logo centré sans overlap)

- **type**: `ui`
- **summary**:
  - Refactor de la navbar en double rendu explicite:
    - bloc mobile/tablette (`xl:hidden`) dédié au pattern compact (burger + logo centré + user + CTA court),
    - bloc desktop (`xl:flex`) pour les actions utilitaires complètes.
  - Suppression des conditions qui mélangeaient comportements mobile/desktop dans une seule rangée.
  - Ajustement largeur/centrage du wordmark pour éviter tout chevauchement avec les zones gauche/droite.
- **files**:
  - `components/layout/Navbar.tsx`
  - `docs/ACTIONS_LOG.md`
  - `docs/logs/2026-03-31.md`
- **why**:
  - Le bug de superposition du logo persistait en mobile réel, nécessitant une séparation structurelle stricte des layouts.
- **impact**:
  - Header mobile plus stable, lisible et cohérent avec la direction premium.
- **verify**:
  - `npm run lint` OK (warning non bloquant existant sur `<img>` dans `components/chatbot/Chatbot.tsx`).
