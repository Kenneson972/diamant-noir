# Journal 2026-04-09

## Cursor — MVP copilot propriétaire (validation prompt)

- **Admin chat** : durcissement Bearer + allowlist (`lib/admin-chat-allowlist.ts`, variables `ADMIN_CHAT_ALLOWED_*`).
- **Owner snapshot** : `tasks_open` dans la réponse GET ; UI liste « Tâches ouvertes » sur `/dashboard/proprio/assistant`.
- **Docs** : `.env.local.example`, RECAP, RECAP_COPILOT, OWNER_ASSISTANT, ACTIONS_LOG.

Vérif : `npm run build`.

### Supabase — migration `owner_alerts_ai_action_logs`

- Projet **DIAMANT NOIR** (`wsdawdxucyuyopkpgjij`) : migration appliquée via MCP `apply_migration` (version enregistrée `20260408152411`, nom `owner_alerts_ai_action_logs`).

### n8n — guide copilot + payload API

- **`docs/n8n/OWNER_COPILOT_AUTOMATION.md`** : flux sans/s avec webhook, contrat JSON, prompt, secret.
- Route **`owner-assistant`** : envoi `context` enrichi (alertes, tâches, villas, stats) vers `N8N_OWNER_WEBHOOK_URL`.
