# Journal 2026-04-07

### Cursor — Audit complet + RECAP + traçabilité (fin de journée)

- **Changement** : rédaction de `docs/audits/audit-complet-2026-04-07.md` (règles Karibloom : mobile, perf, SEO, sécurité, a11y, backend, UI routing) ; mise à jour massive de `RECAP.md` (état d’avancement, chronologie 2026-04-05 → 04-07, section traçabilité ACTIONS_LOG / logs / note sur absence de dumps terminal versionnés, liens prompts Claude Code). Clarification : le « terminal Claude » utile pour l’audit = entrées `agent: claude` dans `ACTIONS_LOG` + `docs/logs/`, pas un fichier de log shell dédié.
- **Vérif** : `npm run build` OK.

### Cursor — Import enrichi : prix non repris

- **Cause** : réponses n8n / LLM avec la clé `price` (non fusionnée) ; nombres avec « € » ; HTML Airbnb parfois en `amountMicros`.
- **Correctifs** : `lib/listing-import-ai.ts` (alias + `coerceNumber` + OpenAI `forceOverride`) ; `lib/listing-import.ts` (regex prix micro / qualifyingPrice).

### Cursor — Supabase MCP + `normalizeImportedAmenities`

- Migration appliquée sur `wsdawdxucyuyopkpgjij` ; import API normalise les équipements vers le catalogue.

### Cursor — Grille catalogue + `amenities_import_labels`

- Migration `20260407120000_villas_amenities_import_labels.sql` : persistance des libellés marqués Import.
- UI : suggestions fixes + personnalisés (Import / Perso).

### Cursor — Badges = libellés importés (`amenities`)

- Pastilles pour chaque équipement, ajout/retrait + détail repliable multi-lignes.

### Cursor — Dashboard : fin des badges prédéfinis

- **Changement** : textarea unique « Équipements » → `amenities` ; retrait des chips PREDEFINED / importés séparés.

### Cursor — Équipements : extérieur ne duplique plus `amenities`

- **Fiche publique** : `equipment_exterior` uniquement ; intérieur = `equipment_interior` ou repli sur `amenities`. Section cachée si rien à afficher.
- **Dashboard** : textes d’aide + titre « Badges incontournables ».

### Cursor — Parse HTML profond : `houseRules` + `Amenity`

- **Constat** : pour `rooms/29571602`, le prix n’apparaît pas dans le HTML SSR (`structuredDisplayPrice":null`, `bookingPrefetchData.price":null`) — uniquement complétion manuelle ou autre source.
- **Correctif** : `extractAirbnbHouseRulesAndAmenities` dans `lib/listing-import.ts` sur les 800 ko du document : règlement (ex. 17:00 / 11:00), 33 équipements `available:true`, warning liste `warnings` si pas de prix.
- **Vérif** : fetch live URL + `npm run build`.
