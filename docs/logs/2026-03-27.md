# Session 2026-03-27

## Cursor — Intégration pack Client Builder

- **Source** : `Downloads/CLIENT BUILDER KARIBLOOM/client-builder-rules/` (rsync vers le workspace).
- **Cible** : `DIAMANTNOIR/.cursor/rules/client-builder/` (37 fichiers : `.mdc` + `docs/REACT_BEST_PRACTICES_RECOMMENDATIONS.md`).
- **Point d’entrée** : `.cursor/rules/karibloom-client-builder.mdc` — section pack, lien `_INDEX.mdc` et `02-stack/`, priorité racine vs pack.

### Vérification

- Présence des dossiers `01-core/` … `07-optional/` et de `_INDEX.mdc`.

### Synchronisation future

- Pour réaligner le pack : `rsync -a "/path/to/client-builder-rules/" "/path/to/DIAMANTNOIR/.cursor/rules/client-builder/"

---

## Cursor — Skills Impeccable (`pbakaus/impeccable`)

- **Source** : [github.com/pbakaus/impeccable — `.cursor/skills`](https://github.com/pbakaus/impeccable/tree/main/.cursor/skills) (clone shallow puis `rsync` vers le workspace).
- **Cible** : `DIAMANTNOIR/.cursor/skills/` (21 skills avec `SKILL.md`, dont `frontend-design` et `critique` avec sous-dossiers `reference/`).

### Mise à jour ultérieure

```bash
git clone --depth 1 https://github.com/pbakaus/impeccable.git /tmp/impeccable-skills-src
rsync -a /tmp/impeccable-skills-src/.cursor/skills/ "/path/to/DIAMANTNOIR/.cursor/skills/"
```

---

## Cursor — Récap projet (`RECAP_PROJET_COMPLET.md`)

- **Source de vérité design** : sortie terminal Claude Code (plan « Upgrade Luxe Premium », fichiers modifiés, puis retour utilisateur fonds blanc/noir logo, hero allégé).
- **Fichier** : `RECAP_PROJET_COMPLET.md` à la racine `DIAMANTNOIR/` — section 4 design détaillée, routes `/`, historique §10, date 27/03/2026.

---

## Cursor — Logo marque réintégré

- **Asset** : `diamant-noir/public/brand/diamant-noir-logo.png`
- **Composant** : `components/layout/BrandLogo.tsx` — variantes clair/sombre (`brightness-0 invert` sur fond sombre pour un logo foncé sur fond transparent).
- **Emplacements** : Navbar, Footer, sidebar espace client, login (desktop + mobile), hero accueil (pictogramme seul au-dessus du bandeau).

---

## Cursor — Inscription propriétaire (`signUp`)

- **Fichiers** : `app/login/page.tsx` (PasswordPanel : onglets connexion / inscription, `signUp` + états succès confirmation email), `app/register/page.tsx` (redirection vers `/login?tab=signup`).
- **Comportement** : Email obligatoire, mot de passe min. 8 caractères côté client, confirmation du mot de passe, nom affichage optionnel (`user_metadata.full_name`). Si Supabase renvoie une session, redirection vers `redirect` (défaut `/dashboard/proprio`) ; sinon écran « Confirmez votre email ».

---

## Cursor — Login : onglets tactiles (Inscription)

- **Problème** : sur mobile, l’onglet Inscription semblait inactif (gris très clair) ou ne répondait pas au tap.
- **Correctifs** (`app/login/page.tsx`) : grille 2 colonnes + `gap-px` (sans div séparateur), `min-h-[48px]`, `touch-manipulation`, `pointer-events-none` sur la vidéo du bandeau, `z-[1]` sur la colonne formulaire, contraste inactif `text-navy/60` + hover léger.

---

## Cursor — Flux locataire : copy allégée

- **Fichier** : `components/auth/TenantMagicLinkFlow.tsx`
- **Changement** : suppression du bloc « tutoriel » (Déjà client / Nouveau compte + étapes), textes OTP/profil/boutons raccourcis, messages d’erreur plus courts, zone démo réduite à un bouton.
- **Pourquoi** : demande utilisateur — interface directe, sans explications longues.

---

## Cursor — Dashboard locataire premium v2 (reprise plan)

- **BookingCard** : image villa en tête (aspect 16:7), badge statut en overlay.
- **ProfileForm** : section photo, upload `profile-avatars`, `avatar_url` dans `user_metadata`, mode démo sans upload.
- **profil/page** : `userId` / `currentAvatar` / `demoMode` ; démo `dn_demo_bypass` avec utilisateur fictif.
- **page espace-client** : hero prochain séjour avec image dynamique depuis la villa.
- **SQL** : `supabase/migrations/20260327120000_profile_avatars_bucket.sql` (bucket + policies, idempotent).

---

## Cursor — Espace client : CSS HeroUI v3

- **Fichier** : `app/espace-client/layout.tsx`
- **Changement** : lien vers `/heroui-v3.min.css` (asset `public/heroui-v3.min.css`) ; état chargement session inclus dans `EspaceClientProviders` avec le même layout.
- **Pourquoi** : sans cette feuille, les classes générées par HeroUI ne matchent pas le CSS du site (Tailwind 3) — composants visuellement « plats ».
- **Vérif** : `npm run build` OK.
