# Journal de session — 2026-05-08

## Dashboard admin — correction plan

- Masquage Navbar/Footer public et chatbot sur `/admin` et `/espace-client`.
- Placeholder gradient pour cartes villas (hub proprio) sans fichier image manquant.
- Nouvelle page `app/(admin)/admin/villas/[id]/page.tsx` + polish shell (header, sidebar, layout, `AdminPageIntro`).
- Formulaire ajout villa : Bearer vers API create-villa, redirect sur `data.id`.
- Sync OTA : `AdminPageIntro` dans la page serveur, titre retiré du composant client pour éviter doublon.
- Fiche membre admin : `AdminPageIntro` + carte sans titre dupliqué.
- Vérification : `tsc --noEmit`, `npm run build` OK.
