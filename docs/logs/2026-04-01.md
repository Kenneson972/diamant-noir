### 2026-04-01T12:00:00Z — Cursor — Règle workflow : vérification localhost (Next.js)

- **type**: `docs`
- **summary**: Mise à jour de `kb-workflow-orchestration.mdc` (racine + `client-builder/01-core/`) : après `npm run build`, pour les changements UI/runtime, vérifier explicitement que `npm run dev` tourne depuis `diamant-noir/`, port libre, purge `.next` si erreur 500, smoke HTTP 200.
- **why**: Éviter « build OK mais localhost cassé » (mauvais cwd, cache `.next`, port occupé).
- **files**: `.cursor/rules/kb-workflow-orchestration.mdc`, `.cursor/rules/client-builder/01-core/kb-workflow-orchestration.mdc`, `CLIENT BUILDER KARIBLOOM/client-builder-rules/01-core/kb-workflow-orchestration.mdc` (pack source agence)

### 2026-04-01T21:30:00Z — Claude Code (terminal) — Vagues prod / a11y / polish

- **type**: `ui` | `perf` | `a11y` | `docs` (session log)
- **summary** (3 vagues, ~7 min — build OK, 44 pages, 0 erreur) :
  - **Vague 1 — Prod + contraste** : `app/globals.css` — `scroll-behavior: smooth` sous `prefers-reduced-motion: no-preference` ; `backdrop-filter` ciblé `.glass-card` + `[data-backdrop]` ; `.eyebrow` opacité ; `.blur-fade-in` + reduced-motion. `app/page.tsx` — trust strip contrastes ; compteur Supabase debug uniquement si `NODE_ENV === 'development'`. `components/booking/CheckoutView.tsx` — session Supabase, `guestName` depuis email invité ; spinners `role="status"` + texte sr-only ; lien « Règles » → `/terms` ; bloc note / avis hardcodé masqué ; nettoyage import `Star`. `app/espace-client/page.tsx` — `text-[9px]` → `text-[10px]`.
  - **Vague 2 — Accessibilité & UX** : `components/layout/Navbar.tsx` — `aria-label` dynamique burger. `components/VillaGallery.tsx` — `alt` thumbnails ; focus lightbox ; `role="dialog"` + `aria-modal` ; `prefers-reduced-motion` sur hover scale. `components/chatbot/Chatbot.tsx` — `<Image>` Next ; fenêtre `role="dialog"` `aria-modal` ; focus FAB à fermeture ; typing indicator respect reduced-motion. `components/espace-client/BookingCard.tsx` — badge `role="status"`. `components/layout/Footer.tsx` — liens `block py-2` ; contraste bottom bar.
  - **Vague 3 — Animations** : `tailwind.config.ts` — keyframes `line-draw`, `stagger-fade`, `gold-shimmer`, `scale-in` + animations. `app/globals.css` — `.stagger-item`, `.line-luxury-animated`, `.text-gold-shimmer`, `.modal-enter` ; désactivation sous `prefers-reduced-motion`.
- **files**: `app/globals.css`, `app/page.tsx`, `components/booking/CheckoutView.tsx`, `app/espace-client/page.tsx`, `components/layout/Navbar.tsx`, `components/VillaGallery.tsx`, `components/chatbot/Chatbot.tsx`, `components/espace-client/BookingCard.tsx`, `components/layout/Footer.tsx`, `tailwind.config.ts`
- **verify**: `npm run build` OK (rapport Claude Code).

### 2026-04-02T00:30:00Z — Claude Code — Doc règle `kb-mobile-responsive` + vérif alignement Diamant

- **type**: `docs`
- **summary** :
  - **Session Claude (terminal)** : mise à jour ciblée de la doc responsive — **backdrop-filter** sur mobile ne doit plus cibler `*` mais `.glass-card`, `.modal-overlay`, `[data-backdrop="true"]` + fond opaque ; **checklist** étendue **14 → 22** items (Fondation / UX & Touch / Performance / iOS Safari) avec points **xs 430px**, **input 16px**, **100vh/dvh**, **safe-area + calc**, **pb sous barre fixe**, **overscroll-behavior**, **reduced-motion**, **tests 375 / 390 / 430px** ; tableau des **7 écarts critiques** (doc métier).
  - **Hors dépôt Git `diamant-noir`** : fichiers workspace `.cursor/rules/kb-mobile-responsive.mdc` + `client-builder/03-architecture/kb-mobile-responsive.mdc` — synchronisés manuellement avec ce contenu (le dépôt GitHub du site ne contient pas `.cursor/`).
- **Vérification appliquée sur le code `diamant-noir`** (extrait) :
  - `tailwind.config.ts` : **`xs: '430px'`** — oui.
  - **`min-h-[44px]`** / tap targets : `SearchResults`, `VillaSelectionCard`, `HeroSearchWidget`, CTA `app/book` — oui.
  - **Safe-area** : `app/globals.css` (`--safe-area-*`, utilitaires), `VillasMapView` sticky `calc(...+env(safe-area-inset-top))`, `Chatbot` FAB / zone saisie, `CheckoutView` padding bas — oui.
  - **Contenu sous barre résa mobile** : `app/villas/[id]/page.tsx` **`pb-[80px] sm:pb-0`** — oui.
  - **Backdrop mobile ciblé** : `app/globals.css` **`.glass-card`**, `[data-backdrop]` — oui (aligné règle).
  - **Viewport** : Next.js injecte le viewport par défaut (App Router) — à confirmer avec balise dans l’HTML généré en prod si besoin client strict.
  - **Pistes restantes** : audit global **font-size 16px** sur tous les champs texte ; **`min-h-dvh`** si bug 100vh iOS sur un écran précis ; **`overscroll-behavior: contain`** sur drawers si pas partout.
- **verify**: Revue grep + lecture `globals.css` / pages concernées ; build avant push.

### 2026-04-01T23:50:00Z — Claude Code (terminal) — Mobile / parcours réservation (P1–P2)

- **type**: `ui` | `a11y` | `mobile`
- **summary** (~6 min — build OK) :
  - **P1 critique** : `tailwind.config.ts` — breakpoint **`xs: '430px'`**. `BookingSearchBar.tsx` — **`xs:flex-row`** (empilé sous 430px). `VillaSelectionCard.tsx` — ratio image `aspect-[4/3]`, paddings responsive, specs colonne mobile, bouton **`min-h-[44px]`**. `CheckoutView.tsx` — paddings `px-4 sm:px-6` / cartes, **sticky CTA mobile**. `globals.css` — attribution Leaflet **9px → 10px**.
  - **P2** : `app/villas/[id]/page.tsx` — **barre réservation sticky mobile** + `pb-[80px] sm:pb-0`. `VillasMapView.tsx` — safe-area sticky, carte **`h-[50vh] min-h-[280px]`**, badge lisibilité. `app/page.tsx` — hero **`min-h-[min(60vh,500px)]`**. `app/book/page.tsx` — hero **`min-h-[60vh]`**, CTA **`flex-col xs:flex-row`** + `w-full xs:w-auto` / **`min-h-11`**. `SearchResults.tsx` — toggles vue **`min-h-[44px]`**, compteur **`text-xs`**, filtres **`shrink-0`** sur chips.
  - **P3** : polish déjà en place (scroll globals, keyframes tailwind, footer) — confirmé inchangé.
- **files**: `tailwind.config.ts`, `components/booking/BookingSearchBar.tsx`, `components/booking/VillaSelectionCard.tsx`, `components/booking/CheckoutView.tsx`, `components/booking/SearchResults.tsx`, `app/globals.css`, `app/page.tsx`, `app/book/page.tsx`, `app/villas/[id]/page.tsx`, `components/VillasMapView.tsx`
- **verify**: `npm run build` OK (rapport Claude Code).
