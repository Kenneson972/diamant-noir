# Session 2026-04-12

## Prestations — popups scroll vidéo (code inline dans la page)

- **Constat** : `/prestations` embarque le canvas + overlays dans `app/prestations/page.tsx` ; les correctifs sur `VideoScrollHero.tsx` n’étaient pas visibles.
- **Changements** : cartes navy/or, typo lisible, mobile ancré en bas avec max-height + scroll, desktop `vertical` par section, JSDoc calibrage frames + `window.__PVSH_LOG_FRAMES` pour logger l’index frame en dev.

## Suite — glass + scroll haut

- Popups **masqués** si `progress` du driver =0 (plus de premier encart sur le hero au retour en haut).
- Cartes **glass** assombries + blur desktop, mobile plus opaque ; ombres portées sur le texte.
- `suppressHydrationWarning` sur les overlays pour limiter les warnings si le bundle client diffère du HTML SSR (vider cache / hard refresh si besoin).
