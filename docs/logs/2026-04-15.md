# Session 2026-04-15

### Cursor

- **Hydratation `/`** : correction **`HeroBackgroundMedia`** — poster SSR + hydra identiques, vidéo après `useEffect` si pas de `prefers-reduced-motion`. Vérif : `npm run build` OK.

- **Villas index** : **`HomeFeaturedAudience`** sans **`ScrollReveal`** sur les cartes (images toujours visibles) ; **`ScrollReveal`** avec timeout de secours ; mapping Supabase images **`trim`** / **`image_urls`**. Vérif : `npm run build` OK.

- **Hydratation BrandLogo / Navbar** : prop **`isDevelopment`** depuis **`app/layout.tsx`** (serveur) → **`SiteFrame` → `Navbar`** ; plus de **`process.env`** pour l’état initial du logo ; classes placeholder = **`BrandLogo`**. Vérif : `npm run build` OK.
