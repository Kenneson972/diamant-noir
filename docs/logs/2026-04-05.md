# Journal 2026-04-05

### Cursor — Plan accueil + landing propriétaires

- Hero : suppression `BookingSearchBar` ; CTA voyageur → `/book` ; `HomeAudienceScroll` et `HomeAudienceGate` alignés sur `/book`.
- `/proprietaires` : progressive disclosure (`<details>`) pour piliers, commission 20 %, liste inclusions (4 + reste) et première location / supplément.
- `EditorialFigureBand` : variante avec légende repliable optionnelle.

**Vérif** : `npm run build` OK.
