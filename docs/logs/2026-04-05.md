# Journal 2026-04-05

### Cursor

- Build : `rm -rf .next && npm run build` après erreurs PageNotFoundError sur routes API ; rebuild OK. `npm run dev` sur http://localhost:3000.
- Hero `/` : révélation animée de `BookingSearchBar` au clic « Réserver un séjour » (mobile) ; View Transition vers `/proprietaires` pour « Confier ma villa ».
