# Journal 2026-04-05

### Claude Code — Mobile hero + typo 9px→10px + screenshots 390px

- **EditorialHeroImmersive** (`components/marketing/editorial-blocks.tsx`) : hauteurs de section et conteneur interne alignées sur `min-h-[400px]` / `xs:min-h-[460px]` / `md:min-h-[88vh]` (au lieu de `88vh` partout → ~743px sur iPhone).
- **Accessibilité / lisibilité** : `text-[9px]` → `text-[10px]` sur l’ensemble des fichiers encore en 9px (dont `NotificationBell`, `VillasView`, assistant proprio, espace client, login, booking, compare villas).
- **Screenshots** : anciens PNG `docs/screenshots/mobile-390px/` supprimés ; nouvelles captures via Playwright (serveur dev ; home refaite avec délai pour la gate audience).
- **Périmètre pages validées en session** : home, propriétaires, prestations, qui-sommes-nous, login/espace client, villas, book.

### Cursor — Supabase : RLS espace client + calendrier + booking-session

- **Migration** `tenant_bookings_rls_calendar_fix.sql` (exécuter sur le projet Supabase : CLI `db push` ou SQL Editor) :
  - Vue `booking_calendar_slots` : `villa_id`, `start_date`, `end_date` uniquement (statuts pending/confirmed dans la vue).
  - `DROP POLICY bookings_public_read` sur `bookings`.
  - Policy `tenant_own_bookings_update` : `UPDATE` pour `guest_email = jwt email`.
  - `support_tickets` : suppression des policies permissives héritées du script OTA ; policy unique par email invité.
- **Code** : calendrier dispo + `BookingForm` → `from("booking_calendar_slots")` ; checklist → `status = confirmed` + date du jour en `YYYY-MM-DD` ; `GET /api/booking-session` → `supabaseAdmin()`.
- **Post-déploiement SQL** : obligatoire pour la prod ; sans migration, la vue n’existe pas et le calendrier échoue.

### Cursor — Header mobile : refonte responsive compacte

- < `sm` : téléphone et favoris masqués pour éviter la surcharge visuelle.
- `sm` → `lg` : téléphone en icône uniquement ; numéro texte gardé pour desktop (`lg+`).
- CTA principal : version icône en mobile/tablette, libellé texte affiché seulement à partir de `md`.
- **Vérif** : `npm run build` OK.

### Cursor — Header mobile : correction du bloc téléphone

- **Navbar** : numéro complet affiché seulement à partir de `lg` (au lieu de `md`) avec `whitespace-nowrap`.
- **Navbar** : icône téléphone conservée sur mobile + tablette (`lg:hidden`) pour garder une cible tactile sans casser la grille.
- **Vérif** : `npm run build` OK.

### Cursor — Ajustement UX gate voyageur (pas de scroll forcé)

- **HomeAudienceGate** : clic **« Je réserve un séjour »** sans navigation `/?pour=locataire` ; on garde la persistance audience + fermeture du gate sur `/`.
- **Objectif** : éviter le comportement de scroll imprévisible sur l’index et rapprocher le ressenti du parcours propriétaire.
- **Validation** : test auto Playwright (`url='/'`, gate démonté, `body overflow: visible`) ; `npm run build` OK ; `npx tsc --noEmit` OK.

### Cursor — Fix parcours voyageur (« Je réserve un séjour » bloqué)

- **Cause racine** : le gate pouvait rester monté en sortie (`opacity-0`) avec `body` verrouillé (`overflow:hidden`) après clic voyageur sur certains cycles ; la garde `mounted` n’était pas réarmée au remontage d’effet.
- **Correctif** : `mounted.current = true` au setup de l’effet de lifecycle dans `HomeAudienceGate`, ce qui permet au timeout `startExit` d’appliquer `setDecision(false)` à chaque cycle.
- **Validation auto** : repro Playwright répétée (6 cycles navbar → changer de parcours → « Je réserve un séjour ») : overlay absent et scroll débloqué après chaque cycle.
- **Vérif** : `npm run build` OK.

### Cursor — Gate : correction cause racine (overlay invisible bloquant)

- **HomeAudienceGate** : quand le gate est visuellement caché (`opacity-0`), l’overlay passe en **`pointer-events-none`** (plus d’interception des clics sur menu/navbar).
- **HomeAudienceGate** : ouverture du gate synchronisée via **`setEntered(show)`** au montage et sur `pageshow` (plus de dépendance à `requestAnimationFrame`).
- **Repro auto Playwright** : cycles répétés « changer de parcours » depuis navbar + scénario footer `/villas` validés après correctif.
- **Vérif** : `npx tsc --noEmit` OK ; `npm run build` OK.

### Cursor — Plan accueil + landing propriétaires

- Hero : suppression `BookingSearchBar` ; CTA voyageur → `/book` ; `HomeAudienceScroll` et `HomeAudienceGate` alignés sur `/book`.
- `/proprietaires` : progressive disclosure (`<details>`) pour piliers, commission 20 %, liste inclusions (4 + reste) et première location / supplément.
- `EditorialFigureBand` : variante avec légende repliable optionnelle.

**Vérif** : `npm run build` OK.

### Cursor — Gate : refresh après plusieurs « Changer de parcours »

- **hydrateAudienceFromUrlIfNeeded** : pas d’hydratation depuis `?pour=` tant que **`dn_gate_reopen_pending`** est actif.
- **replaceHomeAndRequestGateReopen** : second **`requestGateReopen`** en **`requestAnimationFrame`** après tout **`replace("/")`** (y compris nettoyage `/?pour=…`).
- **HomeAudienceGate** : si l’effet voit encore une audience en storage alors qu’une réouverture est en cours, purge du storage.
- **Vérif** : `npx tsc --noEmit` OK.

### Cursor — Gate : `gateReopenSignal` monotonique (sans `consume → 0`)

- Suppression de **`consumeGateReopenSignal`** ; le gate utilise **`lastHandledReopenGen`** pour ne traiter **chaque incrément** qu’une fois (répétitions « Changer de parcours » sans F5).
- **Vérif** : `npx tsc --noEmit` OK.
