# Journal 2026-04-04

### Claude Code (terminal)

- Landing `/proprietaires`, `lib/proprietaires-data.ts`, navigation, sitemap, correctifs build (login, `AvailabilityCalendar`, pas de suppression d’erreurs sur `next.config`).

### Cursor

- `HomeAudienceScroll` : `?pour=proprietaire` → `/proprietaires`.
- `app/page.tsx` : suppression section navy « Programme propriétaires » (contenu sur `/proprietaires` uniquement).
- `/proprietaires` : splits éditoriaux (`EditorialImageSplit`), hero discret (règle or), témoignage avec fond image ; `editorial-blocks` : `eyebrow` / `imageClassName` optionnels sur le split.
- `/proprietaires` (suite) : un seul bloc image+texte pour intro + piliers, inclusions épures, `PROPRIO_LANDING_IMAGES` pour futurs visuels dans `public/proprietaires/`.
- Prompt Superpowers **gate audience accueil** : `docs/superpowers/prompts/claude-code-home-audience-gate.md` (choix voyageur/proprio à l’ouverture, UI/UX Pro Max, animations CSS/Tailwind).

### Claude Code (terminal) — HomeAudienceGate

- `components/home/HomeAudienceGate.tsx` : overlay glassmorphism z-40, sessionStorage `dn_home_audience`, focus trap, Escape + skip, animations CSS/Tailwind uniquement, SSR-safe.
- `app/page.tsx` : import + montage `<HomeAudienceGate />` en premier enfant de `<main>`.

### Claude Code (terminal) — Chatbot public + n8n + plugin frontend-design (session documentée)

Résumé aligné sur la session terminal (import workflow, setup, plugin) :

- **Workflow n8n** : fichier importable `docs/n8n/diamant-noir-chatbot-v1.json` — architecture type CieloBot : webhook `responseMode: responseNode`, nœuds *Build Live Context* (villas Next.js), *AI Agent* + modèle OpenAI, *Parse Response* (JSON + `qualificationScore` / `readyToPrebook`), branches IF (escalade / pré-résa), écritures Supabase avec `continueOnFail`, notification HTTP parallèle, réponse formatée vers le chatbot Next.js.
- **Documentation** : `docs/n8n/SETUP.md` — import du workflow, credentials OpenAI + Supabase, migrations SQL suggérées (`chatbot_leads`, `pre_bookings`), tests `curl`, checklist production, lien avec **`N8N_WEBHOOK_URL`** dans `.env.local` une fois le webhook actif sur l’instance n8n.
- **Plugin Cursor/Claude** : installation du plugin officiel **frontend-design** (Anthropic) via cache `~/.claude/plugins/cache/claude-plugins-official/frontend-design/1.0.0/` + entrée `installed_plugins.json` — skill *frontend-design* pour interfaces distinctives (éviter esthétique générique).
- **Mémoire / suite** : activer le workflow sur n8n ; renseigner `N8N_WEBHOOK_URL=https://…/webhook/diamant-noir-chat` ; tables Supabase à créer si pas encore appliquées.

### Cursor (IDE) — suite 2026-04-04 (hors Claude Code)

- Restauration **login redesign** selon `docs/superpowers/plans/2026-04-02-login-redesign-plan.md` : `app/login/page.tsx` (split 60/40, badge Martinique, champs underline, header texte Diamant Noir, `loginUrlErrorMessage`) ; `components/auth/TenantMagicLinkFlow.tsx` (email + profil sans onglets, style aligné).
- Gate voyageur : `/?pour=locataire` → scroll section `#locataire` sur la home (`HomeAudienceScroll` + `app/page.tsx`).

### Cursor — Catalogue unique vs `/book` (réduction redondance)

- **`/villas`** : point d’entrée principal pour « Réserver » (navbar, accueil, recherche dates) ; bannière si `?checkin=&checkout=` ; ancre `#catalogue` sur le bloc carte/liste.
- **`/book`** : plus de grille `SearchResults` ; texte + CTA vers `/villas` ; si dates sans villa, rappel des dates + lien catalogue ; **checkout Stripe** inchangé (`villaId` + dates en query).
- **Fiche villa** : `id="reserver-sejour"` sur la section calendrier ; barre mobile **Réserver** → ancre `#reserver-sejour`.
- **Stripe** : `cancel_url` → `/villas?canceled=…`

### Cursor — Navbar mobile (anti-chevauchement)

- **`components/layout/Navbar.tsx`** : la colonne logo conserve une `max-width` en `calc(100vw − réserve)` sur `sm` et `md` (au lieu de `sm:max-w-none`), pour laisser la place à téléphone + favoris + CTA texte sans déborder la grille ; `lg:max-w-none` quand la largeur le permet.
