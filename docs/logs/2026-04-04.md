# Journal 2026-04-04

### Claude Code (terminal)

- Landing `/proprietaires`, `lib/proprietaires-data.ts`, navigation, sitemap, correctifs build (login, `AvailabilityCalendar`, pas de suppression d’erreurs sur `next.config`).

### Cursor

- `HomeAudienceScroll` : `?pour=proprietaire` → `/proprietaires`.
- `app/page.tsx` : suppression section navy « Programme propriétaires » (contenu sur `/proprietaires` uniquement).
- `/proprietaires` : splits éditoriaux (`EditorialImageSplit`), hero discret (règle or), témoignage avec fond image ; `editorial-blocks` : `eyebrow` / `imageClassName` optionnels sur le split.
- `/proprietaires` (suite) : un seul bloc image+texte pour intro + piliers, inclusions épures, `PROPRIO_LANDING_IMAGES` pour futurs visuels dans `public/proprietaires/`.
- Prompt Superpowers **gate audience accueil** : `docs/superpowers/prompts/claude-code-home-audience-gate.md` (choix voyageur/proprio à l’ouverture, UI/UX Pro Max, animations CSS/Tailwind).

### Claude Code (terminal) — HomeAudienceGate

- `components/home/HomeAudienceGate.tsx` : overlay glassmorphism z-40, sessionStorage `dn_home_audience`, focus trap, Escape + skip, animations CSS/Tailwind uniquement, SSR-safe.
- `app/page.tsx` : import + montage `<HomeAudienceGate />` en premier enfant de `<main>`.
