# Session 2026-03-30

## Cursor — Intégration HeroUI (espace locataire)

- **Dépendance** : `@heroui/react@3.0.1` — installation avec `npm install @heroui/react --legacy-peer-deps` (conflit peer `react@>=19` et `tailwindcss@>=4`).
- **Styles** : copie de `node_modules/@heroui/styles/dist/heroui.min.css` vers `public/heroui-v3.min.css` ; chargement par `<link rel="stylesheet">` dans `app/espace-client/layout.tsx` pour ne pas passer par PostCSS (incompatible avec les `@layer` Tailwind v4 du bundle).
- **Provider** : `components/espace-client/EspaceClientProviders.tsx` — `I18nProvider` locale `fr-FR` depuis `@heroui/react/rac`.
- **Avatar partagé** : `components/espace-client/TenantAvatar.tsx`.
- **Pages / composants** : accueil espace client, `BookingCard`, messagerie, détail réservation, `ProfileForm` — composants HeroUI (Card, Chip, Skeleton, Separator, Alert, Breadcrumbs, TextField, etc.) ; liens d’action avec `Link` Next + classes `button` du thème HeroUI où les types `Button` ne couvrent pas `href`.
- **Next** : `experimental.optimizePackageImports` inclut `@heroui/react`.

### Vérification

- `npm run build` réussi après intégration.
