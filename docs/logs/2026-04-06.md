# Journal 2026-04-06

## Cursor — Éditeur villa (`/dashboard/proprio/[villaId]`)

- **Changement**: Barre d’onglets réduite à **Planning**, **Contenu**, **Réglages**. Registre réservations déplacé sous le planning ; maintenance + lien analytics multi-villas dans Réglages ; suppression des graphiques Recharts à données fictives sur cette page.
- **Fichiers**: `app/dashboard/proprio/[villaId]/page.tsx`
- **Vérif**: `npm run build` OK.

## Cursor — Éditeur villa : complétude (plan)

- **Changement**: Table des matières + ancres + checklist « avant publication » ; registre des réservations avec recherche, filtres statut/source, export CSV ; lecture `villa_ical_feeds` (affichage + reload après sync) ; bloc **Liens & Sync** avec résumé factuel ; extraction UI vers `components/dashboard/villa-editor/`.
- **Fichiers**: `app/dashboard/proprio/[villaId]/page.tsx`, `components/dashboard/villa-editor/*`
- **Vérif**: `npm run build` OK.

## Cursor — Sync Builder + skill mobile-responsive + audit mobile

- **Changement**: Pack `client-builder` rsync vers `CLIENT BUILDER KARIBLOOM/client-builder-rules/` ; règle pack `kb-mobile-responsive` = redirecteur vers skill `mobile-responsive` ; skill ajouté sous `.cursor/skills/mobile-responsive/` (réf. `full-mobile-guide.md`) + copie dans Builder ; `kb-action-documentation` synchronisée (racine Builder + `01-core`).
- **Audit mobile (code review)**: viewport Next.js + `viewportFit: cover` OK ; body 16px + `.tap-target` OK ; à améliorer : `overflow-x` global optionnel, remplacer certains `100vh`/`min-h-screen` par `svh`/`dvh` sur écrans critiques, `assistant/page` `w-[450px]` rigide, `backdrop-blur` navbar à surveiller sur Chrome Android.
- **Fichiers**: `.cursor/`, `CLIENT BUILDER KARIBLOOM/client-builder-rules/`
- **Vérif**: lecture manuelle des chemins ; build non requis.

## Cursor — Correctifs mobile-responsive (audit)

- **Changement**: `overflow-x: hidden` global ; navbar blur réservé à `md+` ; assistant `100dvh` + layout mobile colonne ; messagerie `100dvh` ; carte villas `100dvh`.
- **Fichiers**: `app/globals.css`, `components/layout/Navbar.tsx`, `app/dashboard/proprio/assistant/page.tsx`, `app/espace-client/messagerie/page.tsx`, `components/VillasMapView.tsx`
- **Vérif**: `npm run build` OK.

## Cursor — Mobile (suite) : fiche villa, tap, overlay menu

- **Changement**: `app/villas/[id]/page.tsx` — `pb-24 sm:pb-0` sur `<main>` pour dégager le contenu au-dessus de la barre réservation fixe mobile. `globals.css` — `touch-action: manipulation` sur boutons / `[role="button"]` / submit. `Navbar` — overlay menu mobile : pas de blur sur petit écran (`md:` seulement), fond `bg-black/50`.
- **Fichiers**: `app/villas/[id]/page.tsx`, `app/globals.css`, `components/layout/Navbar.tsx`
- **Vérif**: `npm run build` OK.

## Cursor — Copy : « Conciergerie privée »

- **Changement**: `titleLabel` accueil / propriétaires + paragraphe `HomeLifestyleAudience` : **Conciergerie privée** (plus « en privée »).
- **Fichiers**: `app/page.tsx`, `app/proprietaires/page.tsx`, `components/home/HomeLifestyleAudience.tsx`
- **Vérif**: `npm run build` OK.

## Cursor — Navbar villas / Leaflet

- **Changement**: Fiche `/villas/[id]` : chrome header comme pages claires (plus texte blanc sur galerie). Z-index site &gt; Leaflet ; lightbox galerie &gt; header ; aperçu rapide catalogue &gt; carte.
- **Fichiers**: `components/layout/Navbar.tsx`, `components/VillaGallery.tsx`, `components/VillaQuickView.tsx`
- **Vérif**: `npm run build` OK ; `npm run dev` relancé.
